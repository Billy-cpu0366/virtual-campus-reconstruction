---
tags:
  - 虚拟校园
  - 计划表
  - 人话入口
  - 总控台
type: plan-dashboard
created: 2026-08-14
updated: 2026-08-22
---

# 项目总控台（人话入口）

> 这页是给 Human 看的一页式总控台。动态状态的机器权威仍是 [`task_plan.md`](task_plan.md)；如果本页与它冲突，以 `task_plan.md` 为准。本页只做同步后的可读视图，不另立任务状态。

## 先看这一屏

- **项目目标**：根据 Peter Oravec 的公开前端和运行表现，恢复系统知识，再用复刻验证理解；不把猜测当事实，不提前建设所谓通用模块。
- **当前阶段**：Phase 1，先完成原站逆向、系统设计和有界复刻。
- **当前工作项**：`WI-THREE-BOARD-VISIBLE-WAVE-001`；03内容、04独立件、05旁支并行，Main另做产品入口和集成。
- **当前权限**：P1报告已闭环，P2视觉目标已接受；统一设计进入clean提交后，四worktree按实现包并行写代码。
- **当前阻塞**：无技术阻塞；等待P2设计提交。
- **本轮目标**：3秒相机+5秒火车、480×270逻辑构图、Memo 6真人引导，并肉眼可见Loading/Retry、真实内容、sprayer、火车和factory smoke；111秒序列禁止进入正常入口。
- **关闭门禁**：自动检查后必须先由Human看实际效果；Human未通过，不得关闭文档。
- **交付边界**：未授权push/PR/Windows同步；GitHub handoff继续暂停。
- **最后同步**：2026-08-22；本页不是自动状态源，发现过期时先回到 `task_plan.md`。

## 项目现在做到哪

| 看哪条线 | 当前结果 | 人话解释 |
|---|---:|---|
| 原站理解 | confirmed 1 / partial 15 / discovered 0 | 地图分块最清楚；P1已补齐App、sprayer、火车和smoke证据，仍保留各自UNKNOWN |
| 详细设计 | 16 / 16 | SYS-APP/NPC/ROUTE/FX已形成P2详细设计；设计完成不等于实现完成 |
| 确定性 CORE | 11 / 16 | Zone、Interact、Game UI新增CORE和真实浏览器接线；仍不是完整系统 |
| 完整系统 | 0 / 16 verified | 每个完整节点仍有明确残余UNKNOWN，不能因有界集成直接晋升 |
| 可玩雏形 | 地图＋玩家＋相机＋内容纵切片已自动验证 | 进入marker可打开DOM内容、暂停/恢复、关闭抑制、离开再进入；完整作品集/NPC/FX未完成 |

> **最容易混淆的一句话**：现在是「11 个 CORE/有界能力已经写了」，不是「11 个完整系统做完」。完整系统仍没有任何一个进入最终 `verified`。

## 16 个系统从哪里看

完整矩阵在 [`01-理解层/00-进度总览.md`](01-理解层/00-进度总览.md)，精确工程状态在 [`03-执行层/00-总账.md`](03-执行层/00-总账.md)。

- **地图线（内部串行）**：资源 → 世界 → 图层 → 分块
- **玩法线（内部串行）**：输入 → 移动 → 玩家 → 相机
- **内容线（内部串行）**：区域触发 → 世界交互
- **独立件（可并行）**：应用启动、游戏 UI、实体生命周期
- **旁支（世界稳定后并行）**：NPC、车辆与路线、动效与粒子

## 一次工作项怎样走完

```text
Human确定目标
  → 理解层查清原站行为
  → 接口层定系统之间交换什么
  → 执行层写事实/推断/决定/未知和验收标准
  → Human书面授权这一次范围
  → 建立一个当前工作项
  → 实现对应代码和测试
  → 按验收标准验证
  → 回写系统卡、总账和task_plan
  → Git提交
```

**没有当前工作项和明确授权，不改正式代码。**

## 文档各自负责什么

| 文件/目录 | 你用它做什么 | 它是不是当前状态权威 |
|---|---|---|
| `计划表.md` | 一眼看懂全局和阅读顺序 | 否，人话视图 |
| `task_plan.md` | 看现在做哪个工作项、能否写、卡在哪里、下一步 | **是，动态状态唯一权威** |
| `决策记录.md` | 看 Human 确认过什么和明确禁止什么 | 是，决定记录权威 |
| `01-理解层/` | 看原站行为和人话系统地图 | 是，理解内容权威 |
| `02-接口层/` | 看系统怎样互相连接和数据口径 | 是，接口语义权威 |
| `03-执行层/` | 看精确事实、设计、失败处理、验收和代码位置 | 是，工程定义权威 |
| `工作日志.md` | 追溯做过什么、遇到什么错误 | 否，只是历史日志 |
| `sample/` | 查原站公开证据 | 是，证据权威；不作为设计决定 |
| `src/`、`game/`、`tests/` | 看正式实现、雏形和自动化测试 | 是，实现与测试权威 |

## 状态怎么读

### 原站理解线

```text
discovered → partial → confirmed
刚发现       懂一半       证据足够确认
```

### 重构工程线

```text
undesign → designed → implemented → verified
没画图纸    图纸定稿     代码已实现      代码和验收都通过
```

### 额外的 CORE 标记

`确定性 CORE 已实现`表示纯逻辑已经有代码和测试；它不等于完整系统已接入网络、Phaser、Tilemap、浏览器和真实资源。

## 发生冲突时怎么处理

1. 当前工作项、权限和下一步：看 `task_plan.md`。
2. Human 是否确认过：看 `决策记录.md`。
3. 原站到底怎么做：看 `sample/` 和对应执行卡的 `FACT`。
4. 我们决定怎么重构：看执行卡的 `DECISION`。
5. 过程日志只用来追溯，不用来判断当前状态。
