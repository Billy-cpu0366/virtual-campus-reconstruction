---
title: 执行层迁移：怎样把精确工程文档归入16张系统卡（治理任务卡）
type: work-item-task-card
status: active
version: v0.1
work-item-id: WI-DOC-EXEC-LAYER-MIGRATION-001
work-item-level: level-1
work-item-type: governance
work-item-status: active
node-refs: not-applicable
authorization-ref: DEC-DOC-EXEC-LAYER-MIGRATION-001
current-gate: none
gate-status: not-applicable
main-definition: false
strict-pa: true
updated: 2026-08-15
---

# 执行层迁移：怎样把精确工程文档归入16张系统卡（治理任务卡）

> **一句话：严格按Human提供的迁移路线，把`doc/v0.1/`的有效工程内容抽进16张执行卡和总账，其余历史完整归档，同时保持代码、证据、测试和当前视觉工作项不丢不改。**

## 1. 来源、目标与基线

- Human执行来源：`执行层迁移路线.md`，SHA-256 `258edd124fd0d4f0e8675f0f039197f4a427a01e9a1c6d4ad3c82e572582ef38`；
- Human指令：`D:\复盘\虚拟校园项目重构\执行层迁移路线.md 按照这个文档继续做迁移`；
- 安全基线：`1fabc8a9df780ba9d2b4e078db10f8a8b61d94fc`，当时本地HEAD与`origin/main`一致；
- 目标：`03-执行层/`形成16张系统卡、`00-总账.md`和操作手册；`doc/v0.1/`最终只保留当前视觉工作项与重定向README；历史进入`migration-history/doc-v0.1/`。

## 2. 允许范围

1. 按路线建立16张执行卡，3张从正式设计、调查记录和验证计划抽取实质内容，13张建立空槽；
2. 建立`03-执行层/00-总账.md`和重写`03-执行层/README.md`；
3. 从旧机器台账抽取仍生效Human决定与签字历史到根`决策记录.md`，并补根README的Phase 1边界；
4. 将`doc/v0.1/`除当前视觉任务卡外的内容按原结构移入`migration-history/doc-v0.1/`；
5. 更新`AGENTS.md`、根README、当前任务状态、治理记录和必要指针；
6. 每个路线步骤形成独立提交，最后执行结构、链接、测试、类型和Git边界验证。

## 3. 明确禁止

- 不修改、移动或新增`sample/`、`src/`、`tests/`中的任何字节；
- 不修改旧Phaser项目、公开证据、正式代码或测试逻辑；
- 不填补路线明确保留的相机、资源加载和24层视觉TBD；
- 不归档、删除或改写当前视觉补证任务卡的实质内容；迁移期间只允许同步其`paused/active`状态；
- 不丢失旧文档，不压平`migration-history/doc-v0.1/`目录结构；
- 不force-push、不改写Git历史；本任务默认不推送，除非Human另行要求。

## 4. 冲突消解

路线同时要求“检查器归档”和“`sample/`一个字节不碰、最终diff为空”。硬约束与验收优先：

- `sample/tools/check-doc-governance.py`原文件保持逐字节不动；
- 第4步在`migration-history/doc-v0.1/`保存一份历史快照，作为旧治理检查器归档；
- 新结构使用本任务的专用只读结构检查，不改旧检查器来适配新结构。

路线中“当前工作项就差5张截图”是过期进度描述；权威状态表明5场景证据已经完成并等待Human审查。本任务只暂停和恢复审查，不重新采集、不改变证据结论。

## 5. 执行步骤

0. 记录安全基线、Human来源、accepted决定并原子激活本治理工作项；
1. 建16张卡：3张有肉、13张空槽；
2. 建总账和操作手册；
3. 归位顶层决策与Phase 1边界；
4. 归档历史壳和旧检查器快照；
5. 将`doc/v0.1/`收敛为当前视觉任务卡和重定向README；
6. 修复`AGENTS.md`与根README的执行入口；
7. 执行完整验收、独立复核、关闭本工作项并恢复视觉审查。

## 6. 退出标准

- 路线第六节验收清单全部通过；
- 16张卡与理解层一一对应，3张有肉卡7格完整，13张空槽格式一致；
- `doc/v0.1/`仅一份重定向README和当前视觉任务卡；
- 历史文档按原结构可定位，源路线与旧检查器可恢复；
- `sample/src/tests`自安全基线差异为0，26项测试和typecheck通过；
- 当前状态、Human决定、指针和归档边界同步；
- clean结果提交和独立只读复核通过，视觉工作项恢复到Human审查。

## 7. 停止条件

遇到以下任一情况立即停止，不扩大范围：旧文档无法无损归档、16卡映射冲突、必须修改`sample/src/tests`才能继续、当前视觉任务卡会丢失、或Git出现无法解释的既有差异。

## 8. 分步结果

| 步骤 | 状态 | 结果 |
|---|---|---|
| 0 安全网与激活 | passed | 提交`4f339b8`；来源哈希、安全基线、accepted决定和唯一当前治理工作项已进入clean基线 |
| 1 建16张卡 | passed | 提交`4f886f5`；3张designed有肉卡、13张空槽卡与理解层路径一一对应 |
| 2 总账和操作手册 | passed | 提交`e02db16`；16系统双状态、验证差距、未知队列、6条规矩和6步循环已落盘 |
| 3 顶层归位 | passed | 提交`3e0689b`；当前Human决定/签字历史与Phase 1边界已归位 |
| 4 历史归档 | passed-preflight | `doc/v0.1`的46份历史Markdown按原结构移动，逐文件SHA-256一致；旧检查器快照与原文件哈希均为`97d6ac9a...32f83c`；当前视觉任务卡保持原路径 |
