---
tags:
  - 虚拟校园
  - 决策记录
type: decision-log
created: 2026-08-14
updated: 2026-08-20
---

# 决策记录（Human 决定当前台账）

> 这里维护"仍生效"的 Human 决定和签字历史。每个决定保留稳定 ID、Human 签字原文、日期、适用范围、当前效力/完成结果和明确未授权边界，方便随时审计。
> 已替代或已完成的决定只进签字历史，不再作为当前规则；动态状态（当前工作项、阶段、Gate、下一步）唯一权威仍是根 `task_plan.md`，本文件不复制。

## 权威与历史位置

- **动态当前状态**（当前工作项、阶段、Gate、下一步）：唯一权威是根 [`task_plan.md`](task_plan.md)。
- **当前精确执行知识**：`03-执行层/`（16 张系统卡 + 总账 + 操作手册）。
- **完整旧机器合同**（含全部来源登记、替代关系、持久化与验证状态）：第 4 步归档后位于 `migration-history/doc-v0.1/谁确认过什么（来源与决策记录）.md`，只作历史可查，不再是当前权威。

## 仍生效的 Human 决定

| 稳定 ID | 决定内容 | 签字原文 / 日期 | 适用范围 | 当前效力 / 完成结果 | 明确未授权边界 |
|---|---|---|---|---|---|
| `DEC-DOC-GOV-001` | 文档治理：四色标记（FACT/INFERRED/DECISION/UNKNOWN）、理解与工程两条状态线分开、单一事实源、结论落地才算做完、Human Gate | `把这个给填充进去文档…`；`ok，开始吧`；`ok`（2026-08-10） | 项目文档与结论持久化 | accepted；规则已并入 `03-执行层/README.md` 六条规矩 | 不含旧 Phaser、正式 src 或复用提取 |
| `DEC-WORK-RELAY-001` | 轻量任务接力：节点与工作项分离、滚动选择、分级工作项、关键 Human Gate、单次原子激活 | `确认，这个方案开始执行吧`；`继续`（2026-08-10） | 正式工作项选择、授权、执行、阻塞和关闭 | accepted；SYS-CHUNK CORE 已完成首次真实试点 | 内部小步骤不单独建 WI；不制造虚假选择工作项 |
| `DEC-WORK-RELAY-002` | 允许零个或一个当前正式工作项；无预授权下一项时进入合法选择状态 | `ok，开始修改吧`（2026-08-11） | 工作项关闭后没有唯一预授权下一项的情况 | accepted；CORE 已合法关闭并进入选择状态 | 不选择下一工作项；不授权新代码或范围扩大 |
| `DEC-SYS-CHUNK-001` | SYS-CHUNK 详细设计晋升唯一正式主定义 | `通过`（2026-08-10） | SYS-CHUNK 详细设计 | accepted；后续实现由 CORE 授权约束 | 实现仍需独立授权 |
| `DEC-SYS-CHUNK-CORE-001` | 实现授权：Node.js 22 LTS、npm、TypeScript strict、Vitest；master 契约、行优先索引、坐标/边界换算、玩家3×3、相机+1、目标集合与单元测试 | `ok`（2026-08-11） | `WI-SYS-CHUNK-CORE-001` | accepted 且 verified；`src/chunk/` 已实现，26 项测试、typecheck、审计均通过 | Phaser/Vite、网络、缓存、渲染、旧项目修改未授权 |
| `DEC-SYS-WORLD-LAYER-INVESTIGATION-001` | 世界装配与图层边界调查（level-1） | `我接受这个方案…`（2026-08-11） | `WI-SYS-WORLD-LAYER-DESIGN-001` | accepted 且 verified | 不含正式代码、证据刷新、复用提取 |
| `DEC-SYS-WORLD-LAYER-DESIGN-001` | `SYS-WORLD` 与 `SYS-LAYER` 晋升唯一正式主定义（24层、master+chunk 单一运行真相、对称清除、生命周期边界） | `按当前设计定稿，并允许下一项做最小视觉补证。`（2026-08-11） | `SYS-WORLD`、`SYS-LAYER` 详细设计 | accepted 且 verified；当时未关闭Q-LAYER-001，后续已由视觉证据关闭并拆出Q-LAYER-002 | design-only；不含实现 |
| `DEC-SYS-LAYER-VISUAL-EVIDENCE-001` | 最小视觉补证：公开原站 5 场景截图与 JSON 状态采样 | `按当前设计定稿，并允许下一项做最小视觉补证。`（2026-08-11）；证据结论`通过`（2026-08-15） | `WI-SYS-LAYER-VISUAL-EVIDENCE-001` | accepted 且 verified；5 场景证据 Human 已通过并关闭工作项；particles3 残余转 `Q-LAYER-002` | 仅白名单场景；不含正式代码、不扩大镜像 |
| `DEC-DOC-PORTAL-MIGRATION-001` | 总文档入口单仓迁移：复制人话层、修正同仓指针、建立统一入口并验证 | `…去按照这上面说的做一个文档迁移`；`ok。开始把`（2026-08-14） | `WI-DOC-PORTAL-MIGRATION-001` | accepted 且 verified | 不含正式 src、证据重组、旧 Phaser、来源目录删除 |
| `DEC-DOC-PORTAL-CLEANUP-001` | 旧总文档目录收敛为单一跳转 README，普通推送 GitHub | `ok，开始执行第八步和推送github吧`（2026-08-14） | `WI-DOC-PORTAL-CLEANUP-001` | accepted 且 verified；本地与 `origin/main` 一致 | 不含删除目录本身、force-push、Git 历史改写、Phase 2 搬迁 |
| `DEC-DOC-EXEC-LAYER-MIGRATION-001` | 执行层 16 卡 + 总账 + 操作手册迁移，旧 doc/v0.1 原结构归档 | `…按照这个文档继续做迁移`（2026-08-15） | `WI-DOC-EXEC-LAYER-MIGRATION-001` | accepted、persisted、verified；clean结果`293cbeb2d9bcf99f28c2a7cb62de10ee0e08f0c5`通过结构、链接、26项测试、typecheck、Git边界和独立复核 | 不含 `sample/src/tests`、旧 Phaser、证据结论、TBD 补全、force-push |
| `DEC-SYS-ASSET-INVESTIGATION-001` | SYS-ASSET 资源加载详细逆向：查证原站资源加载机制（加载什么/什么时候/失败/缓存）并填卡成设计候选（level-1 调查） | `ok。那就先开始这个资源板块部分吧`；`开始`（2026-08-15） | `WI-SYS-ASSET-DESIGN-001` | accepted；查证与填卡已完成，BASE-ASSET-001/002/003/004 确认 | 不写正式 `src/`、不扩大镜像采集、不碰其他系统、不碰内容 TBD、不定义通用 Loader |
| `DEC-SYS-ASSET-DESIGN-001` | SYS-ASSET 资源加载详细设计晋升唯一正式主定义 | `通过`（2026-08-15） | `SYS-ASSET` 详细设计 | accepted | 实现仍需独立授权；不定义通用 Loader |
| `DEC-SYS-INPUT-INVESTIGATION-001` | SYS-INPUT 输入详细逆向：查证原站输入机制（键盘映射/摇杆插件/三输入归一化/单指跟踪）并填卡成设计候选（level-1 调查） | `换下一个系统逆向吧`；`开始`（2026-08-15） | `WI-SYS-INPUT-DESIGN-001` | accepted；查证与填卡已完成，BASE-INPUT-001/002/003/004 确认 | 不写正式 `src/`、不扩大镜像采集、不碰其他系统、不碰内容 TBD |
| `DEC-SYS-INPUT-DESIGN-001` | SYS-INPUT 输入详细设计晋升唯一正式主定义 | `通过`（2026-08-15） | `SYS-INPUT` 详细设计 | accepted | 实现仍需独立授权 |
| `DEC-SYS-MOVE-INVESTIGATION-001` | SYS-MOVE 移动与碰撞详细逆向：查证原站移动/碰撞机制（碰撞体尺寸/墙层碰撞/walls-layer.json 寻路数据/分轴来源/卡墙反馈）并填卡成设计候选（level-1 调查） | `把玩法大概摸清`；`开始`（2026-08-15） | `WI-SYS-MOVE-DESIGN-001` | accepted；查证与填卡已完成，BASE-COLLISION-001/002/003/004 确认 | 不写正式 `src/`、不扩大镜像采集、不碰其他系统、不碰内容 TBD |
| `DEC-SYS-MOVE-DESIGN-001` | SYS-MOVE 移动与碰撞详细设计晋升唯一正式主定义 | `通过`（2026-08-15） | `SYS-MOVE` 详细设计 | accepted | 实现仍需独立授权 |
| `DEC-SYS-PLAYER-INVESTIGATION-001` | SYS-PLAYER 玩家详细逆向：查证原站玩家外观/朝向/动画状态机/出生点/深度/换装并填卡成设计候选（level-1 调查） | `ok，开始吧`（2026-08-15） | `WI-SYS-PLAYER-DESIGN-001` | accepted；查证与填卡已完成，BASE-PLAYER-001/002/003/004 确认 | 不写正式 `src/`、不扩大镜像采集、不碰其他系统、不碰内容 TBD |
| `DEC-SYS-PLAYER-DESIGN-001` | SYS-PLAYER 玩家详细设计晋升唯一正式主定义 | `通过`（2026-08-15） | `SYS-PLAYER` 详细设计 | accepted | 实现仍需独立授权 |
| `DEC-SYS-CAMERA-INVESTIGATION-001` | SYS-CAMERA 相机详细逆向：查证原站相机跟随/边界/缩放/开场航拍序列并填卡成设计候选（level-1 调查） | `开始吧`（2026-08-15） | `WI-SYS-CAMERA-DESIGN-001` | accepted；查证与填卡已完成，BASE-CAMERA-001/002/003/004 确认 | 不写正式 `src/`、不扩大镜像采集、不碰其他系统、不碰内容 TBD |
| `DEC-SYS-CAMERA-DESIGN-001` | SYS-CAMERA 相机详细设计晋升唯一正式主定义 | `通过`（2026-08-15） | `SYS-CAMERA` 详细设计 | accepted | 实现仍需独立授权 |
| `DEC-DOC-TERM-001` | 术语规范化：`02-暗号层`→`02-接口层`、`接头暗号表`→`API 契约表`、术语「接头/暗号」→「接口」 | `那个文档当中的暗号层我认为不规范`；选择「接口层 + API 契约表」（2026-08-15） | 当前工作链全部 md 文档术语 | accepted；40 文件已替换、一致性脚本 PASS | 不含 `migration-history` 历史归档、不含接口实现代码 |
| `DEC-SYS-ASSET-CORE-001` | SYS-ASSET 实现授权：确定性纯逻辑 CORE（资源 URL 约定、切块瓦片发现、加载 key 构造）；失败处理照抄原站（只 `console.error`，不重试/不降级）；TypeScript strict + Vitest | `A`；`开工`（2026-08-16） | `WI-SYS-ASSET-CORE-001` | accepted 且 verified；`src/asset/` 已实现，typecheck + 11 项本系统测试通过 | Phaser/Vite、网络请求、缓存、重试、降级、渲染、旧项目修改未授权 |
| `DEC-SYS-LAYER-CORE-001` | SYS-LAYER 实现授权：确定性纯逻辑 CORE（24 层策略表、玩家深度公式、桥上下/屋顶淡隐/墙壁碰撞 GID/脚印深度）；玩家深度以 SYS-LAYER 卡 `500+(y+24)*0.1` 为准（契约表/SYS-PLAYER 的 `500+y*0.1` 待对账）；TypeScript strict + Vitest | `开工`（2026-08-16） | `WI-SYS-LAYER-CORE-001` | accepted 且 verified；`src/layer/` 已实现，typecheck + 12 项测试通过 | Phaser/Vite、网络、缓存、渲染、Tilemap 写入、旧项目修改未授权 |
| `DEC-SYS-WORLD-CORE-001` | SYS-WORLD 实现授权：确定性纯逻辑 CORE（世界规格从 master 派生、生命周期 ready/destroyed 状态机、chunk 结构校验、原子放块/撤块记账、幂等 destroy、失败回滚）；层语义仍归 SYS-LAYER 世界只消费；TypeScript strict + Vitest | `开始`（2026-08-16） | `WI-SYS-WORLD-CORE-001` | accepted 且 verified；`src/world/` 已实现，typecheck + 30 项本系统测试通过 | Phaser/Vite、网络、缓存、渲染、Tilemap 真实写入、碰撞重算、旧项目修改未授权 |
| `DEC-SYS-INPUT-CORE-001` | SYS-INPUT 实现授权：确定性纯逻辑 CORE（8 方向与向量换算、速度 150/106、键盘上>下>左>右归一化、摇杆主轴 1.5 归一化与参数表、摇杆激活让键盘位）；摇杆阈值 0.3/0.5 单位 UNKNOWN 只登记不断言；TypeScript strict + Vitest | `ok`（2026-08-16） | `WI-SYS-INPUT-CORE-001` | accepted 且 verified；`src/input/` 已实现，typecheck + 23 项测试通过 | Phaser/Vite、网络、缓存、渲染、rexVirtualJoystick 插件集成、真实键鼠/触摸监听、旧项目修改未授权 |
| `DEC-SYS-MOVE-CORE-001` | SYS-MOVE 实现授权：确定性纯逻辑 CORE（玩家碰撞体 20×8/偏移 14,36/drag 300/maxVel 200、方向→速度组合、撞墙反馈方向→blocked 轴映射、碰撞墙层 5 层清单、walls-layer 0/1 寻路网格）；速度 150/106 引用 SYS-INPUT；层名/深度 SSOT 在 SYS-LAYER；TypeScript strict + Vitest | `开始`（2026-08-16） | `WI-SYS-MOVE-CORE-001` | accepted 且 verified；`src/move/` 已实现，typecheck + 8 项测试通过 | Phaser/Vite、网络、缓存、渲染、Arcade 物理引擎集成、分轴碰撞、真实 body 注册、旧项目修改未授权 |
| `DEC-SYS-PLAYER-CORE-001` | SYS-PLAYER 实现授权：确定性纯逻辑 CORE（出生点 (1088,304)、外观 48×48/64 帧 8 方向×8 帧/frameRate 10/帧区间映射/idle 帧 48、空闲状态机 30s 坐下/8s 小动作/排除上一个、朝向默认朝南、换装与被抓尺寸 48×48↔64×64/冷却 1s）；深度归 SYS-LAYER 不重复定义；TypeScript strict + Vitest | `继续`（2026-08-16） | `WI-SYS-PLAYER-CORE-001` | accepted 且 verified；`src/player/` 已实现，typecheck + 13 项测试通过 | Phaser/Vite、网络、缓存、渲染、Phaser Sprite/动画创建、真实贴图加载、被抓/换装/传送完整流程、旧项目修改未授权 |
| `DEC-SYS-CAMERA-CORE-001` | SYS-CAMERA 实现授权：确定性纯逻辑 CORE（边界 2240×2240/缩放 1/硬跟随 lerp=1/offset·deadzone 0/roundPixels/物理 30 FPS、开场航拍 6 点序列与总时长约 111s、结束 tween 3s Power2、nativeScale 换算 blur·scaleFactor·分块）；nativeScale 为运行时注入非写死参数；TypeScript strict + Vitest | `继续`（2026-08-16） | `WI-SYS-CAMERA-CORE-001` | accepted 且 verified；`src/camera/` 已实现，typecheck + 10 项本系统测试通过；后续渲染授权已消费该结果 | Phaser/Vite、网络、缓存、渲染、真实相机/tween/后处理管线创建、航拍逐帧推进机制、nativeScale 注入来源、旧项目修改未授权 |
| `DEC-RENDER-PLAYABLE-001` | 联网+渲染第一步（可玩雏形）：Vite 7 + Phaser 3.90.0（用原站同款 `public/vendor/phaser.min.js` 全局脚本，避免 npm phaser@4 大版本破坏）；渲染 `final_map.json` 的 layer1–10（SYS-LAYER 深度策略）、玩家出生/8 方向走路动画/空闲（SYS-PLAYER 帧映射）、键盘 WASD+方向键→方向→速度（SYS-INPUT/SYS-MOVE）、相机边界/缩放/硬跟随/roundPixels（SYS-CAMERA）、动态深度（SYS-LAYER `playerDepth`）；只消费 8 个确定性 CORE，不改其源码 | `ok，直接开始联网渲染让我看看效果`（2026-08-16）；`我现在验收通过了`（2026-08-19） | `WI-RENDER-PLAYABLE-001` | accepted、persisted、automated-verified、human-visual-verified；`npm run build`、133项测试、编译产物 preview、browser Smoke 均通过，Human 已确认画面可接受；结果基线 `7c5a738` | 分块流式加载（master+chunk HTTP）、Arcade 分轴碰撞/wall 碰撞、NPC/交互/粒子/FX、换装/被抓/传送完整流程、屋顶淡隐/桥深度切换、旧项目修改、src/ 确定性 CORE 改动未授权 |
| `DEC-SYS-CHUNK-WORLD-INTEGRATION-001` | 实现动态分块运行时：使用 master + chunk JSON 作为运行数据；Chunk 负责目标集合、请求、缓存、在途去重、失败/过期状态；World 负责 validated chunk 的原子 apply/remove、回滚和销毁；Phaser 只通过适配层消费两者；不复制旧 Bundle 的不完整图层卸载路径 | `ok，我授权了，开始吧，设定计划一步步实现`（2026-08-19） | `WI-SYS-CHUNK-WORLD-INTEGRATION-001` | accepted、persisted、automated-verified、human-visual-verified（Human：“视觉验收通过”，2026-08-20）；首版视觉验收因 `exterior` firstgid 注册错误及 Tiled 空 GID 写入兼容问题未通过；两项修复与验证器页面清理已完成，干净预览 4199 的 browser Smoke 与跨块 Smoke 通过；本次有界动态分块集成已通过视觉 Gate，结果提交 `b707553` | 允许修改 `src/chunk/`、`src/world/`、`game/`、相关 `tests/`、`scripts/`、`package.json` 和派生 `public/`；不修改 `sample/`、旧 Phaser 项目；不纳入粒子/NPC/交互/完整特殊13层语义；最终需 Human 运行验收 |
| `DEC-VERIFY-CURRENT-WORK-ITEM-001` | 增加只读的当前工作项关闭验证入口：按 core / integration / visual-integration / docs-evidence 类型分流；统一检查状态与授权，按类型执行 typecheck、测试、构建、运行资源和浏览器验收要求；机器输出不得替代视觉 Human Gate | `ok。开始增加吧`（2026-08-17） | `WI-VERIFY-CURRENT-WORK-ITEM-001` | accepted；implementation in-progress | 不修改 `src/`、`game/`、`sample/` 或旧 Phaser；不新增角色、模板或全项目重审；不自动签署 Human Gate、不自动改状态或提交 |
| `DEC-DOC-CONTROL-REPAIR-001` | 文档总控修复：恢复 `计划表.md` 为一页式人话总控；分开记录理解状态、完整系统工程状态和确定性 CORE 状态；统一玩家深度公式、SYS-WORLD测试计数与任务状态入口 | `ok。开始吧`（2026-08-18） | `WI-DOC-CONTROL-REPAIR-001` | accepted、persisted、verified；结果提交 `c40f6df` 通过总控、链接、状态一致性和Git边界检查 | 仅修改总控/进度/接口/系统卡和状态文档；不修改 `src/`、`game/`、`public/`、`sample/`；不修运行时；不自动授权或关闭后续资源、浏览器和安全工作项 |
| `DEC-RESOURCE-REPRO-001` | 资源可复现性调查：盘点游戏入口、地图、tileset、public资源、Git忽略规则和构建入口，比较纳入版本控制与构建时生成方案；调查阶段不直接改资源或代码 | `ok。开始吧`（2026-08-18） | `WI-RESOURCE-REPRO-001` | accepted、persisted、verified；结果提交 `f8a9014` 通过资源清单、外部tileset对比、状态和Git边界检查 | 不修改 `src/`、`game/`、`public/`、`sample/`；不下载新资源；不修地图；方案 A/B/C 需Human选择后，资源实现重新授权 |
| `DEC-RESOURCE-IMPLEMENT-001` | 方案 A 资源可复现实现：从仓库内 sample 公开镜像复制4个运行资源，生成当前可玩雏形使用的 sanitized 地图，接入 `dev/build/preview`，增加资源完整性检查；粒子 tileset 不纳入本次实现 | `A`（2026-08-18） | `WI-RESOURCE-IMPLEMENT-001` | accepted、persisted、verified；结果提交 `6815a6f` 通过资源检查、typecheck、133项测试、build和干净检出验证 | 仅修改 `scripts/`、`package.json` 和被忽略的派生 `public/`；不修改 `src/`、`game/`、`sample/`；不纳入粒子tileset；不推送远端 |
| `DEC-BROWSER-STARTUP-001` | 浏览器启动与入口验证：把 `game/` 纳入 TypeScript 检查，启动真实页面并记录 Tilemap、资源请求、运行时错误和视口结果；只修入口验证缺口，不处理粒子完整渲染 | `继续`（2026-08-18） | `WI-BROWSER-STARTUP-001` | accepted、persisted、automated-verified；`ffa97d0` 通过 game typecheck，`7c5a738` 固化 browser Smoke；自动阶段已关闭，visual Gate 转交 `WI-RENDER-PLAYABLE-001` | 仅修改 `game/`、`tsconfig.json`、必要 `scripts/`/命令入口；不修改 `src/`、`public/`、`sample/`；不自动签署视觉 Gate |
| `DEC-GIT-PR-WORKFLOW-001` | 第二位协作者正式加入后，吸收 PR #2 的核心协作规则：独立分支/worktree、main保护、变更前后状态检查、预览/验证后Human确认、明确push/PR边界、创建PR后停止；不原样合并PR #2，不引入重复Todo状态源 | `确实是引入第二位协作者了现在要进行协作了`（2026-08-18） | `WI-DOC-COLLABORATION-ADOPT-001` | accepted、persisted、verified；结果提交 `9bd475b`；协作规则已写入 `AGENTS.md` 和 `03-执行层/README.md` | 仅修改 `AGENTS.md`、`03-执行层/README.md`、`task_plan.md`、`计划表.md`、`决策记录.md`、`工作日志.md`；不修改 `src/`、`game/`、`public/`、`sample/`；不自动合并或push任何PR |
| `DEC-API-COLLABORATION-001` | 吸收 PR #3 的 API 协作审查理念：外部规范是待核验输入/交付收据，不是项目事实源；以 `02-接口层/API契约表.md`、`03-执行层/` 系统卡、代码/测试和 `task_plan.md` 逐层对照；接口契约状态与工程实现状态分开记录；`Core Ready` 不等于 `Integrated`；未确定的 Caller/Provider 方向保持 `UNKNOWN`/`Draft` | `算了，就直接参考她的理念吧，我觉得他的方向确实是对的，看看怎么融合进目前工作流`（2026-08-18） | `WI-API-COLLABORATION-REVIEW-001` | accepted、persisted、verified；结果提交 `6da5755`；流程已融合，未新增 API 事实 | 仅修改 `AGENTS.md`、`03-执行层/README.md`、`task_plan.md`、`计划表.md`、`决策记录.md`、`工作日志.md`；不复制不可核验外部文档；不修改 `02-接口层/API契约表.md`、系统卡、`src/`、`game/`、`sample/` |

| `DEC-VERIFY-PORT-001` | 验证入口统一使用 4175：Vite dev/preview 开启 strictPort，browser Smoke 默认访问 4175 | `统一到 4175（推荐）`（2026-08-20） | 当前动态分块视觉 Gate 的本地验证入口 | accepted、persisted、automated-verified；typecheck、145项测试、资源检查、状态一致性和build通过 | 不改变游戏逻辑、地图资源、Human视觉签署和远端服务 |
| `DEC-SYS-MOVE-WORLD-COLLISION-001` | 墙体与桥真实碰撞集成：在已验收的 master+chunk 动态地图上接入玩家 Arcade Body（20×8、offset 14,36、drag 300、maxVelocity 200）、walls 与四个 bridge wall 碰撞层、`body.blocked` 撞墙反馈和 chunk 装卸后的碰撞清理；碰撞主责归 SYS-MOVE，图层/世界接入协同 SYS-LAYER、SYS-WORLD、SYS-CHUNK；桥状态来源采用公开 Bundle 已证实的 tile 矩形边沿触发，不猜造其他触发器 | `ok。开始吧`（2026-08-20） | `WI-SYS-MOVE-WORLD-COLLISION-001` | accepted、persisted、verified；实现提交 `e3f412a`；typecheck、147项测试、build、碰撞/桥/跨块/普通浏览器 Smoke通过 | 允许修改 `game/`、必要 `src/move/`、`src/layer/`、`src/world/`、`tests/`、验证脚本和相关文档；不修改 `sample/`、旧 Phaser 项目；不纳入手机摇杆、NPC、完整13层、交互、粒子或其他未授权玩法；4175旧服务不作为本次证据，本次当前 build 使用4200显式验证 |
| `DEC-RUNTIME-SAFETY-001` | 运行时安全有界修复：处理 World 同步/异步 hook 在当前层部分写入后抛错时的补偿回滚；生产 build 不安装可见诊断面板和调试 hooks，开发诊断限制日志数量；初始化 rejected、renderer teardown rejected 有显式收敛；入口以 inline favicon 消除默认 404 | `运行时安全审计（推荐）`（2026-08-20） | `WI-RUNTIME-SAFETY-001` | accepted、persisted、verified；实现提交 `632a0c9`；149项测试、普通 production build、安全/普通/跨块 Smoke、test-hooks 碰撞 Smoke通过 | 允许修改 `src/world/`、`game/`、`index.html`、必要 `tests/`、`scripts/`、`package.json` 和相关文档；不修改 `sample/`、旧 Phaser 项目；不扩展完整 Phaser resource teardown、完整图层、粒子、NPC、交互、验证器或远端 Git；碰撞 Smoke 的 hooks 只允许出现在显式 `test-hooks` build，不进入普通 production build |

## Phase 1 范围（Human 已确认）

- **目标**：基于 Peter Oravec 公开前端恢复系统知识，并以原站复刻验证理解；复用成果从实际实践中总结，不预设。详见根 [README.md](README.md) 的"Phase 1 边界"小节。
- **纳入**：网站部分（页面启动/结构导航/作品卡牌弹窗/游戏嵌入/响应式适配）；虚拟世界部分优先（资源加载；地图/分块/图层/碰撞；玩家/输入/移动/相机；NPC/实体/车辆/路线；区域触发/交互/UI/动效）；公共支撑（内容配置；资源路径命名；状态事件通信；错误兼容调试）。
- **不在范围**：未公开源码、私有仓库和 Git 历史；服务端、数据库、密钥；未在公开文件或运行时出现的猜测资源；最终校园业务、后台和多人在线；为证明复用提前建设第二套内容。
- **证据边界**：只读 `sample/` 公开镜像与运行时证据；现有 Phaser 复刻仅作实现对照，不是事实源。
- **复用**：第二个独立真实场景出现相同行为或同一逻辑被重复修改时才登记观察；未经 Human 确认不得自行抽象。

## 签字历史（Gate 与已替代/已完成决定）

| 历史 ID | 内容 | 签字原文 | 日期 | 状态 |
|---|---|---|---|---|
| `GATE-DOC-FRAMEWORK` | 文档框架验收 | Human 通过（依据 task_plan 与进度记录） | — | passed |
| `GATE-STAGE6A` | 现有复刻代码全局盘点 | Human 通过（依据 task_plan 与阶段基线） | — | passed |
| `GATE-STAGE6B` | P0 差距、调查顺序与地图分块建议 | `开始` | 2026-08-10 | passed |
| `GATE-STAGE6C` | 首个系统详细逆向与设计（SYS-CHUNK） | `通过` | 2026-08-10 | passed |
| `GATE-GOV-SYNC` | DEC-DOC-GOV-001 第二轮权威同步 | `ok` | 2026-08-10 | completed |
| `GATE-WORK-RELAY-ADOPTION` | 轻量任务接力采用与首次试点准备 | 确认方案 | 2026-08-10 | pilot-verified |
| `GATE-GOV-PILOT` | 治理机制试点 | CORE 实现、验证、关闭闭环 | 2026-08-11 | passed |
| `GATE-SYS-CHUNK-IMPLEMENTATION` | SYS-CHUNK CORE 正式实现授权 | `ok` | 2026-08-11 | passed（Phaser 继续未授权） |
| `DEC-DOC-GOV-000` | 文档完整拆解与复用治理提案 | 未独立接受 | 2026-08-08 | superseded（被 `DEC-DOC-GOV-001` 替代） |
| `DEC-STAGE6B-001` | 选择 SYS-CHUNK 并启动阶段6C | `开始` | 2026-08-10 | 已完成（阶段 6C 已通过） |
| `DEC-DOC-NAV-001` | 人话优先文档导航 | `把人话放在前面， 技术目录序号放在括号里面` | 2026-08-10 | 部分替代；人话优先原则继续并入执行层规则 |
| `DEC-DOC-NAV-002` | 正式文件人话标题 | `下一轮可以继续把每个正式文件也改为…` | 2026-08-10 | 已完成（命名约定保留） |
| `DEC-DOC-NAV-003` | 文档接力说明与导航图 | `所以目前这样一个路径…` | 2026-08-10 | 已完成（导航材料随 doc/v0.1 归档） |
| `DEC-DOC-NAV-004` | 一级目录数字前置排序 | `所以首先把各个部分的文件夹名字改变一下…` | 2026-08-11 | 已完成（doc/v0.1 结构归档后不再适用） |
| `DEC-DOC-NAV-005` | SYS-CHUNK 纵向人话解读 | `ok，开始吧，包括建立一个用人话解读的文档…` | 2026-08-11 | 已完成（解读文档随 doc/v0.1 归档） |

> 来源登记、替代关系、持久化/验证状态等完整合同见第 4 步归档后的机器台账；本文件不再复制。
