# 工作日志

> 本文件是非权威过程日志，由原 `findings.md`（发现记录）、`progress.md`（进度记录）与 `task_plan.md` 的「错误记录」表合并而来。
> 当前阶段与 Human Gate 只以 `task_plan.md` 为准；规则、Human 决定和签字历史分别以 `AGENTS.md` 与根 `决策记录.md` 为准；正式工程结论以 `03-执行层/` 对应系统卡为准。旧 `doc/v0.1` 只作历史（现归档于 `migration-history/doc-v0.1/`）。
> 本文件只追加记录过程，不复制任何状态。

## 执行历史（原 progress.md）

- 已建立原 `samples/`（现为 `sample/`）目录骨架、空清单模板与分析占位文档。
- 已启动公开发布文件参考包建立任务；尚未下载任何原站文件。
- 已启用 Mode A 子 agent：新增 `recon`、`lightweight-worker`、`lightweight-verifier`，均使用官方 `openai-codex/gpt-5.6-terra:medium`。
- Recon 已确认镜像路径规则和首批 6 个地图 JSON 入口；现有旧下载器覆盖不完整，不能直接视为全量采集器。
- Mechanical worker 已完成第一批 6 个 JSON 的白名单采集，全部 HTTP 200；已生成可复核 manifest、Network 索引、空缺失清单和独立采集脚本。
- 已补正 manifest，使其每条记录均可独立表达 URL 到 mirror 路径的映射；已验证 6 个磁盘文件的 SHA-256 与清单一致。
- 第二批地图直接依赖已采集：18 个成功；`exterior.png` 为原站 HTTP 404，已明确记录为不可用而未落盘。
- 第三批已根据 TSX 的唯一显式引用采集 `tileset-particles.png`；当前镜像有 25 个成功文件、1 个已记录的 404，全部成功文件哈希已核对。
- 第四批已采集原站根 HTML 到 `mirror/index.html`；未请求其子资源。根页记录为 HTTP 200、86,024 bytes、SHA-256 `65375062...507855`，既有 25 个镜像文件字节保持不变。
- 第五批已根据修正后的 HTML 静态引用清单采集 47 个同源资源，全部 HTTP 200；当前 manifest 共 73 个成功文件，唯一 unavailable 仍为 `exterior.png` 的 HTTP 404。所有成功文件字节与 SHA-256 已复核。
- 第六批已采集 CSS 的 10 个可用静态资源；`card5_foil.webp`、`cables3.png` 为 HTTP 404。脚本现已默认同时保留成功和 unavailable 快照，后续批次不再重复请求已确认 404，除非显式 `--refresh`。
- 第七批已采集应用 JS 的 14 个明确依赖，全部 HTTP 200；当前有 97 个成功镜像文件与 3 个已记录 404，成功文件的哈希/大小、清单一致性和快照保护均已复核。
- 第八批已从动态 gameplay bundle 的 141 个完整静态资源字面量生成白名单并采集，全部 HTTP 200。当前有 238 个成功镜像文件、3 个已记录 404；全量文件哈希/大小与 manifest 一致。
- 已建立并运行隔离 Chrome/CDP 运行时 Network 采集工具，覆盖导航、Ready、Play 与四方向移动；未影响日常 Chrome。首轮记录 404 次响应（去重后待分析），输出当前位于 `sample/analysis/runtime-network.json`。
- 第九批已补采 25 个真实请求的地图 chunk 与 2 个 UI 资源，全部成功。`sample/` 采集和内部一致性校验已完成：265 个成功文件、3 个 404、镜像/manifest/resource-index/unavailable 可双向对应。 
- 已建立 `doc/v0.1/` 初始框架：需求分析、概要设计、详细设计、验证、逆向计划以及3份方案说明，初始共22份 Markdown；结构与相对链接自检通过，后续已完成 Human 验收。
- 已将旧项目移至 `D:\虚拟校园项目旧资料`，将当前重构项目迁入 Obsidian Vault 的 `D:\复盘\虚拟校园项目重构`；迁移前后299个文件逐项大小和 SHA-256 一致，原位置已清理。三份当前方案说明也已从旧资料移入当前版本文档目录。
- 已将本轮收敛结论写入 AI 必读链：不预设可复用系统、实践重复后记录、Human 批准后提取；同时加入唯一 Human Gate、按需门禁触发表、中央复用观察表和四类详细设计模板提醒。22份文档无空文件、相对链接缺失为0。
- 已补齐 `01-目标与范围/这次要复刻什么（目标与范围）.md`：明确原站复刻是第一套实践场景、复用成果不限于用户示例、真实重复后才观察和提取、未来校园真实使用才证明跨项目复用；章节和必需规则自检通过。
- 已完成框架一致性修正：三份辅助说明不再提前指定复用能力，功能总目录（节点清单）移除预建的路线跟随能力，未知问题保留为实际机制调查，地图分块状态统一为 pending，验收状态改为直观中文；独立复核6项全部 PASS，无阻塞缺陷。
- 已按单仓库结构整理项目：`samples/` 改为 `sample/`，采集工具移入 `sample/tools/`，22份当前文档移入 `doc/v0.1/`，建立空 `src/` 入口；移动后逐文件校验275个样本文件、2个工具文件和22个文档文件一致。
- 已在当前项目根初始化 Git 仓库，默认分支为 `main`；已绑定 Private GitHub 仓库 `Billy-cpu0366/virtual-campus-reconstruction` 并推送。`.gitattributes` 已禁止 Git 转换公开证据文件，270个证据文件的仓库对象与工作区字节一致。
- 已补齐复用与解耦闭环：观察表增加批准/提取状态和唯一主归属，系统模板增加状态所有权、公开接口、调用/事件边界和依赖方向，四类模板统一同步观察表，并补充异步失败触发、三表状态口径和 P0 行为基准准入；独立审查 PASS，无阻塞缺陷。
- Human 已确认 `doc/v0.1/` 文档框架验收通过；框架入口、分类说明、模板和目标范围晋升为 `approved`，持续演进的原站逆向内容继续保持 `draft`。当前进入首个系统选择阶段，尚未开始正式源码。
- 已启动阶段1“现有复刻代码全局盘点”：任务计划已落盘，范围限定为只读确认代码基线、模块全景和可运行行为；禁止修改或迁移旧 Phaser 项目，也不写正式 `src/`。
- 阶段1已确认权威旧代码基线为 `origin/main@aa2ab7e`；当前M3 Worktree提交 `3c27b81` 与其完整Git tree相同，验证前后tracked状态均为clean，GitHub远程main也已用`ls-remote`确认。
- 阶段1命令验证全部通过：`npm ci`、typecheck、Phaser静态smoke、M3地图合约和Vite生产构建均exit 0；构建存在约1.5MB主JS的大Chunk警告。
- 阶段1浏览器验证通过：桌面4x与移动2x内部buffer、Ready→Play→HUD、键盘移动、墙体阻挡、相机跟随与右边界均有实际结果；Runtime异常0，已知唯一HTTP失败为Sandbox `/favicon.ico` 404。
- 已新增 `02-整体怎么运作/旧版本现在做到什么（现有实现盘点）.md` 和 `04-怎么验证与还差什么/旧版本实际表现是什么（行为基线）.md`，并更新概要/验证入口与证据追踪矩阵；当前 `doc/v0.1/` 共24份Markdown。
- 阶段1首轮结构检查通过：24份Markdown为14份approved框架与10份draft演进内容，链接缺失0、围栏异常0、版本异常0；复用观察保持“未发现”。独立复核同时发现旧仓库其他Worktree存在未提交Rust历史WIP，因此暂停交付并补充全局Worktree清单；阶段1遗留的8197/8198服务进程已清理。
- 全部14个旧Worktree已完成只读清单：3处dirty分别是主clone的B107/Rust校准WIP、b1-08 Loading WIP和assets-audit未跟踪planning输出，其余11处clean；没有清理、合并或修改这些WIP。补充独立复核PASS，无阻塞缺陷。
- Human 已确认阶段6A现有复刻代码全局盘点通过；两份基线文档晋升为`approved`，提交`53f091a`已推送GitHub。当前进入阶段6B原站系统与现有代码差距映射，仍禁止修改旧代码或写正式`src/`。
- 阶段6B已完成P0初步对照并新增`02-整体怎么运作/先做什么（P0系统对照与顺序）.md`：建议先详细逆向地图分块，再推进世界/图层、分块所需加载、玩家/输入/移动和相机。
- 地图分块结构化复核已证明：5×5个28×28 chunk按`index=y*5+x`重组后，24层与`final_map.json`逐格完全一致；Bundle证明按玩家邻域与相机范围动态加载和卸载。Q-MAP-001、Q-MAP-002已关闭。
- Q-MAP-003已关闭：Play前相机序列通过`preloadChunksForCameraSequence`覆盖当前5×5地图并空闲预载全部25块；进入玩家阶段后再按玩家邻域和相机范围动态装卸。Network时间口径已修正为chunk自身跨度约0.930秒、master至最后chunk约1.536秒。
- 阶段6B最终独立复核PASS：P0系统对照、25块重组、Bundle动态装卸、三项未知关闭和DECISION/proposed边界均可重复验证，sample/src零修改。阶段6B已完成并等待Human审查，阶段6C和正式详细设计尚未开始。
- 已将阶段6B审查包单独提交为`4b87ac742acf1bd1bb86273f5da855b18a4bdb5a`；提交只保存待审材料，不代表Human Gate通过。
- Human 已授权执行 `DEC-DOC-GOV-001` 第二轮权威同步；当前只修改治理文档、模板、状态检查和验证记录，不推进阶段6B/6C，不修改旧Phaser或正式`src`。
- Human 以`开始`通过阶段6B并授权阶段6C的SYS-CHUNK有界详细逆向。Gate迁移已提交`9cd7895`；已建立`03-具体怎么做/系统/地图分块：从原站查到了什么（SYS-CHUNK 调查记录）.md`，仍为非主定义、`undesign`，未修改旧Phaser或正式`src`。独立证据复核发现并修正13个未清除图层的计数；修正后在干净提交`4af281b`上复核PASS。
- 已提交`6a397a0`形成`地图分块：玩家移动时怎样加载地图（SYS-CHUNK）-详细设计候选.md`：状态所有权、重复请求、失败、取消、销毁和验收均为 `DECISION（proposed）`，待 Human 审查；节点仍为`undesign`，正式实现仍未授权。
- Human 已通过 SYS-CHUNK 详细设计：正式主定义为`03-具体怎么做/系统/地图分块：玩家移动时怎样加载地图（SYS-CHUNK）.md#SYS-CHUNK`，节点升为`designed`，验证计划已建立；当前转入`GATE-SYS-CHUNK-IMPLEMENTATION`等待独立 Human 实现授权，未写正式`src`。
- Human 已明确接受 `DEC-WORK-RELAY-001` 轻量任务接力方案并授权开始首次真实试点同步。本轮建立稳定接力规则、`WI-SYS-CHUNK-CORE-001` 的 awaiting-authorization 任务卡和 proposed 一页授权包，修正已知动态状态漂移并扩展最小检查器；本轮不授权正式 `src`、技术栈或 Phaser 集成。
- 轻量接力首次同步 dirty transaction preflight 已通过：Python内存编译 PASS，治理 `sync` 854项 PASS，`pilot` 859项 PASS，`git diff --check` PASS；正式源码和证据快照零修改，`src/README.md` 只修正未授权状态说明。lightweight-verifier只读复核PASS，无blocking/high/medium缺陷。
- 轻量接力制度和 proposed CORE 试点已提交为 `e9c0c552673a5d32d7dea860978c4a214ebac12a`；提交后工作区 clean，`sync` 840项、`pilot` 845项结构复验PASS。当前达到 `sync-verified`。
- Human 已通过 `DEC-SYS-CHUNK-CORE-001`；原子激活提交为 `981a21e54ed7cd3eac3ec3bc5b26ce12bd8086a4`，激活验证落盘提交为 `cb7ae0e9d386aee9045c8ebddab057a263d483cd`，activation/sync/pilot 均在 clean 基线上通过。
- CORE 实现提交为 `f04568f953821e8cc56c33a694171ddab759051f`：TypeScript strict PASS，3文件26项测试 PASS，npm 官方源 audit 0漏洞，治理 sync 835项/pilot 840项 PASS；最终 lightweight-verifier PASS，无 blocking/high/medium/low 缺陷，`sample/` 和旧 Phaser 零修改。
- 已完成设计、授权包、验证计划、证据与差距、工程入口和过程记录回写；节点继续保持 `designed`，因为 Phaser、请求、缓存、渲染和生命周期未实现。
- 工作项关闭被 `BLK-WI-CLOSURE-001` 阻塞：无预授权下一项时，协议和检查器缺少合法的“无当前工作项/等待选择”状态。等待 Human 接受最小空闲合同或直接选择下一工作项。
- Human 已接受 `DEC-WORK-RELAY-002` 最小修补；已将 CORE 关闭、task_plan 切换为 `current-work-item: none / work-item-selection`，并让检查器按零或一个当前 WI 条件检查。dirty transaction preflight：Python内存编译 PASS、sync 851项 PASS、pilot 856项 PASS、`git diff --check` PASS；该计数包含按脏工作树变更路径执行的写入边界检查。
- 最小关闭合同内容提交为 `df93d97f2ff8dfb88d54dd58f2d58351bd7c0720`；clean 基线上 Python compile、sync 840项、pilot 845项和 `git diff --check` 均 PASS。lightweight-verifier 只读复核 PASS，无 blocking/high/medium 缺陷；CORE 工作项已合法关闭，当前等待下一项选择。
- Human 已要求按阅读顺序重命名01至05一级目录，并以SYS-CHUNK建立第一份纵向人话解读。目录、内部引用、机器入口和治理检查器已同步；新建`用地图分块看懂01到05（SYS-CHUNK 人话解读）.md`并接入项目入口与03入口。dirty工作树结构检查为sync 916项、pilot 921项PASS。
- 导航内容提交为`71919ca`，Python缓存清理提交为`85586f4`；在clean内容基线`85586f4`上完成sync 878项、pilot 883项、`git diff --check`、旧路径扫描和Main语义复验，`DEC-DOC-NAV-004/005`进入verified。
- Human已接受世界装配与图层边界作为下一level-1工作项。已建立`WI-SYS-WORLD-LAYER-DESIGN-001`调查任务卡，task_plan进入active/investigation且保持无代码Gate；同步修正检查器将level-1误当实施授权包的合同缺口，正式代码和旧项目继续未授权。
- level-1原子激活提交为`dd199de16fcf4dbe5ded678c9d7df218a7cd5832`；clean基线上Python内存编译、sync 909项、pilot 914项和`git diff --check`通过，lightweight-verifier独立复核PASS，节点继续保持undesign。
- 世界与图层调查第一步已由低成本Subagent并行完成：mechanical-worker验证25个chunk的24层结构一致且与final_map逐格差异0；recon定位空世界、tileset、层创建/depth、chunk写入/清除、roof/bridge/cars/footsteps和卸载锚点。Main随后抽查关键Bundle与地图证据，确认优化默认、22层清单、两套depth公式和11层卸载边界。
- 已建立`世界与地图：从原站查到了什么（SYS-WORLD 调查记录）.md`和`图层与遮挡：从原站查到了什么（SYS-LAYER 调查记录）.md`，均为`main-definition: false / engineering-status: undesign`；当前仍在调查阶段，尚未形成正式设计或代码授权。
- 第二轮低成本调查完成：mechanical-worker证明footsteps grid与tilelayer位置逐格一致，并量化particle layers与88个trajectory regions的有限关联；recon确认特殊13层无额外Tile清空、默认优化分支22层、无显式idle取消/Tilemap destroy及玩家动态depth覆盖。Main已完成关键计数和坐标抽查。
- 已建立SYS-WORLD与SYS-LAYER两份`system-design-candidate`及各自验证计划，工作项进入design阶段。候选建议master+chunk单一运行真相、24层完整策略和对称清除；`Q-LAYER-001`继续open，等待Human决定是否单独授权最小视觉补证。节点仍为undesign，正式代码仍未授权。
- dirty工作树独立客观复核9项PASS，并发现Recon把普通分支全层遍历误报为可写全部24层；Main核对`o=["layer1"]`守卫后确认实际只写layer1，已修正所有受影响文档。
- Human已明确接受当前SYS-WORLD与SYS-LAYER设计并授权下一项最小视觉补证。两份候选已晋升为`system-detail / accepted / main-definition: true`，节点总账同步为designed；下一任务卡已建立为preauthorized，正式代码仍未授权。
- 正式设计与接力包dirty独立复核PASS：两个accepted决定、两个唯一主定义、关键地图/Bundle事实、Q-LAYER open、5场景预授权任务卡和保护路径均正确；sync 1019项、pilot 1024项、`git diff --check`通过。
- clean设计内容提交`8c7fff7525e8dd77c6367b662f65fec12175d33f`通过sync 1001项、pilot 1006项、diff检查和独立只读复核。`WI-SYS-WORLD-LAYER-DESIGN-001`已关闭。
- `WI-SYS-LAYER-VISUAL-EVIDENCE-001`原子激活提交`51b6f72f46b2cb3713eaaac8dcdc3050fe39bbbe`通过sync 1012项、pilot 1017项、diff检查和独立复核；HEAD含active状态且工作区clean。
- 视觉补证采集已执行：公开原站HTTP 200、intro完成、13个阶段状态和13张截图生成；layer8遮挡、factory roof淡隐、bridge1上下切换、particles3区域表现和footsteps生成均得到运行证据。JSON确定性断言PASS；正式代码和旧Phaser零修改。
- Main已逐张检查13张截图，确认上层灌木遮挡、factory屋顶显隐、bridge通行画面、抗议区域和脚印轨迹与JSON状态能互相解释。独立客观复核PASS，并将particles3直接消费者保持为Q-LAYER-002 UNKNOWN。
- clean结果提交`f1652629d436ce7f8a7821c760036fdf071ef397`通过JSON 209项客观断言、sync 1017项、pilot 1022项和diff检查；视觉补证结果已验证，当前停在Human证据结论审查，工作项尚未关闭。
- Human已授权按`D:\复盘\虚拟校园项目·总文档\00-迁移结合路线.md`执行总文档单仓迁移。第0步发现的既有表格格式化差异已独立提交为`a2e303b`；准备提交`714774b`建立治理任务卡、accepted决定和纯治理工作项最小合同，激活提交`7d65f4f`暂停视觉审查并原子激活迁移。
- 原样导入提交`383d2a8`新增26份人话文档，复制前后SHA-256差异0且`doc/sample/src/tests`零变更。随后已完成同仓指针、历史兄弟目录路径、根README、SSOT声明和AGENTS人话入口改造。
- 迁移预验证：27份入口文档共19个Markdown内联链接缺失0；11个关键执行/素材/历史物理路径全部存在；错误旧仓库路径和称呼残留0；来源仍保留28份Markdown。`npm test`为3文件26项PASS，`npm run typecheck`PASS；dirty治理`sync`1068项、`pilot`1073项PASS。
- clean验证基线`cda98173a24df1b605019d3b7126ea092dd4b6cf`通过sync1064项、pilot1069项、26项测试、typecheck、链接/路径/Git边界检查和lightweight-verifier只读复核；无blocking/high/medium/low缺陷。final closure基线`f23f3f43859ddf944868acea76f5a8a312a71b93`恢复视觉证据Human审查并通过sync1074项、pilot1079项。
- Human随后授权第8步和GitHub推送。清理准备提交`d1e192a`把原路线与迁移前README按原字节归档到Git并建立accepted决定；激活提交`3911b55`暂停视觉审查并激活`WI-DOC-PORTAL-CLEANUP-001`。
- 第8步已执行：旧总文档目录从28份Markdown收敛为一份跳转README、子目录0，旧目录本身保留。clean结果提交`e037559`通过sync1066项、pilot1071项、26项测试、typecheck、恢复/边界检查和lightweight-verifier独立复核；两份原件位于`migration-history/`且哈希不变。
- 首次普通push因远端新增PR #1 merge提交`971d2a3`被拒，未使用force。fetch审计确认该远端提交与共同基线`03fe5ac`tree相同、内容差异0；无内容merge提交`2824d78`安全接入远端拓扑。final closure`2ff5b98390dfe5c528cbc384f2f83dfe15e8cc47`通过clean sync1075项、pilot1080项并与`origin/main`一致；清理工作项关闭，视觉证据Human审查恢复。
- 2026-08-15 Human要求按`执行层迁移路线.md`继续迁移。路线第0步激活提交`4f339b8`记录来源哈希、安全基线、accepted决定和`WI-DOC-EXEC-LAYER-MIGRATION-001`；视觉证据Human审查短暂暂停，证据与任务卡实质内容保持原位。
- 路线第1步已完成并提交为`4f886f5`：`03-执行层/`新增3张有肉卡和13张空槽卡，路径与`01-理解层`16卡完全一致；脚本检查16卡、7节、极简frontmatter、3/13状态和无旧wiki链接全部PASS，`sample/src/tests`零变化。
- 路线第2步已提交为`e02db16`：新增`03-执行层/00-总账.md`，保留16系统理解/工程双状态线、验证差距和未知队列；`03-执行层/README.md`成为含6条规矩、6步循环、16卡导航和动态权威说明的操作手册。
- 路线第3步已提交为`3e0689b`：根`决策记录.md`抽取仍生效决定与签字历史并认主当前Human决定，根README新增完整Phase 1边界并修正旧总文档过期描述；第6步AI执行入口尚未提前切换。
- 路线第4步已提交为`7bc7054`：46份旧`doc/v0.1` Markdown按原结构移动到`migration-history/doc-v0.1/`，逐文件哈希一致；当前视觉任务卡原位保留。旧治理检查器在历史区保存同哈希快照，`sample/tools`原文件未动；`task_plan.workflow-ref`已切到现操作手册。
- 路线第5步已提交为`1953796`：`doc/v0.1/README.md`成为兼容跳转页，旧目录只剩README和当前视觉任务卡两个文件；当前/历史入口均可定位。
- 路线第6步已提交为`139a0dd`：AGENTS和根README当前入口已切到`03-执行层/`、根决策与task_plan；API 契约表/术语表和3张有肉卡的旧源指针同步修复，task_plan进入verification。
- 路线第7步本地clean验收通过：18份执行层文档、16卡一一对应、3/13状态和7节完整；doc仅2文件；46份历史齐全且检查器快照同哈希；当前60份Markdown链接缺失0；26项测试与typecheck PASS；Git对象正常，`sample/src/tests`自`1fabc8a`零差异。clean结果提交`293cbeb`通过lightweight-verifier独立10项复核，无blocking/high/medium/low缺陷；迁移关闭并恢复视觉证据Human审查。

## 调查发现（原 findings.md）

- 当前参考站：`https://peteroravec.com/`。
- 公开发布文件可作为逆向材料；完整原始项目源码不属于爬取目标。
- 当前已有 Phaser 实现位于 `C:\Users\inertnet\peter-oravec-clone-worktrees\m3-01-map-assets\`，本阶段只读盘点，不修改或迁移。
- 阶段1基线初查：该 worktree 位于旧仓库分支 `task/m3-01-map-assets`，提交 `3c27b81165056b947d1c64a9543445ed81e92a3d`，与 `origin/task/m3-01-map-assets` 同步且工作区 clean；旧仓库远程为 `Billy-cpu0366/virtual-campus-prototype`。
- 旧仓库同时保留 main、M2碰撞、M2移动、M3地图资产和迁移实验等多个 worktree。`m3-01-map-assets` 不能仅凭名称直接视为全部历史进展的唯一集成版，必须先比较分支关系和实际差异；本阶段不会合并这些分支。
- `m3-01-map-assets` 当前 tracked 文件185个；源码按 runtime、scenes、world、player、input、camera、ui、dev sandboxes 分层，另有地图合约、构建、冒烟、截图和审计脚本。`node_modules/` 与 `dist/` 已存在，但均未显示为 Git 修改。
- 分支关系复核：远程集成分支最新提交为 `origin/main` 的 `aa2ab7e841ad4749bc63219d3d2e46ce50d4b193`（M3地图资产合约 PR #5）；`task/m3-01-map-assets` 的 `3c27b81` 是同一父提交 `5b04e05` 上的PR提交版本。两者完整 Git tree hash 均为 `bcfb60b0d30c42214f4a1c94526506038bc31855`，因此阶段1以 `origin/main@aa2ab7e` 作为权威集成提交，以当前 clean 的 M3 worktree作为字节等价可执行快照。
- M2碰撞与移动分支保留了未压平的开发历史，但其最终播放器碰撞体、移动速度和八方向动画结果已进入当前集成树；它们不是需要在本阶段合并的缺失功能。M3新增的关键内容是地图资源合约脚本与粒子占位GID清理规则。
- 旧仓库 `.planning/2026-08-01-clone-portfolio/` 是更早的 Rust/WASM 方案历史，里面的阶段状态与当前 Phaser 3 + Vite + TypeScript 实现不一致，只能作历史背景，不能作为当前代码状态源。
- 当前运行入口由 `src/main.ts` 组合 DOM UI 与单一 `GameHandle`；runtime负责单实例替换、resize、状态与销毁；`CampusScene` 是生产组合点；world/player/input/camera为独立核心模块；DOM UI仅通过生命周期接口控制 Loading、Ready、Starting、Playing、Error。
- 当前地图实现加载一份 `final_map_phaser.json` 与17个tileset，创建10个可见图层和隐藏walls碰撞层；它没有使用 `master.json` 或25个运行时chunk，因此尚未实现原站地图分块装载机制。地图中的动态粒子占位GID会在创建Tilemap前被清零，粒子行为明确后置。
- 当前玩家实现包含WASD/方向键、归一化八方向输入、150速度、八方向动画、20x8脚部碰撞体、世界边界和墙体碰撞；相机提供0.1平滑跟随、世界边界与像素取整。Ready和Playing均允许移动，这是旧仓库显式决定，不应自动当作原站事实。（注意：旧复刻的"相机0.1平滑跟随"≠原站行为——原站是硬跟随 `startFollow(player,true,1,1)` lerp=1，见 SYS-CAMERA 卡。）
- 当前UI具备加载封面、进度、Play淡出、HUD、启动错误和整页刷新Retry；没有实现标记、弹窗交互、NPC、车辆或其他gameplay features。Core Sandbox提供world/player/collision/camera四种聚焦验证视图，复用生产模块而非第二套实现。
- `npm run smoke` 是静态合约检查：验证关键文件、加载样式、地图尺寸/tileset/碰撞标记、玩家参数、模块根目录和跨模块导入，但不启动浏览器。`scripts/m3-01-map-contract.mjs` 进一步验证地图尺寸、10个可见层、隐藏walls、17个tileset、GID范围、粒子占位和图片尺寸。阶段1仍需独立浏览器行为验证。
- `scripts/smoke-test-result.md` 与 `scripts/b14-smoke-test.cjs` 属早期Rust/WASM/B1历史，前者记录cargo/trunk，后者仍探测`window.wasmBindings`；它们不能代表当前Phaser集成树的现状。当前有效检查以package scripts、M3地图合约和重新执行的浏览器基线为准。
- 当前Sandbox开发页会请求未提供的 `/favicon.ico` 并得到HTTP 404；不影响地图、输入、碰撞或相机，但违反严格零网络失败基准，属于现有实现的非阻塞缺口，后续不能将其误报为全零404。
- 阶段1命令复验全部通过：Node `v22.17.1`、npm `11.7.0`；`npm ci`、`npm run typecheck`、`npm run smoke`、`node scripts/m3-01-map-contract.mjs`、`npm run build` 均退出0。Vite构建36个模块，主JS约1495.54kB（gzip约344.82kB），触发大于500kB的chunk警告；这是当前性能/分包风险，不是构建失败。
- 主流程浏览器复验通过：1920x1080桌面Canvas显示尺寸1920x1080、内部buffer 480x270（4x）；390x844移动视口Canvas显示尺寸390x844、内部buffer 195x422（2x）；两者均完成Ready→Play→HUD，浏览器异常0，控制台只有Phaser 3.90启动日志。
- Sandbox浏览器复验 `STAGE1_SANDBOX_BROWSER_PASS`：camera模式玩家从(1048,300)右移到(1153,300)，相机从(1048,300)跟随到(1144,300)；collision模式从(1128,1928)向下只移动4px停在y=1932，向上可移动到y=1872；右边界玩家被世界边界限制在x=2230，相机midpoint限制在x=2000；world模式不创建玩家。浏览器异常0，唯一HTTP失败是已记录的favicon 404。
- 执行安装、检查、构建和浏览器验证前后，旧 worktree 的 tracked 工作区均保持 clean；临时截图、JSON和测试脚本只写入系统临时目录，没有进入旧仓库或新仓库 `src/`。
- `git ls-remote origin refs/heads/main` 返回 `aa2ab7e841ad4749bc63219d3d2e46ce50d4b193`，确认本地记录的权威集成提交与GitHub远程当前main一致，不是过期remote-tracking ref。
- 当前24个地图层中，运行时仅创建`layer1`至`layer10`和隐藏`walls`；未创建`cars`、4个roof、4个bridge wall、3个particles和`footsteps`共13层。是否应显示或由动态系统消费仍需按原站证据逐项判断。
- GitHub Code CI只执行`npm ci`、typecheck、静态smoke、build和git diff；未执行M3地图合约或浏览器/Sandbox验证。M3合约有效但未挂入package scripts/CI。
- 其他现有风险：生产bundle主JS约1.5MB且无代码分割；`vite base: "/"`与绝对资源路径假定部署在站点根；`sourcemap: true`会发布source map；index仍包含Google Fonts和Peter原站SEO/外链；`getUiElements()`在Game创建前失败时不会进入现有Error UI；Loader事件监听未显式解绑；sanitizer会原地修改tilemap cache，同Game内Scene重启时无法恢复被清零GID。这些是当前代码风险，不代表都应在下一圈立即修复。
- 全部14个旧Worktree只读状态复核发现3处dirty：主clone `C:/Users/inertnet/peter-oravec-clone` 有5个tracked修改和9个未跟踪B107/probe脚本；`b1-08`有`index.html`与`www/js/main.js`两处tracked修改；`phaser-assets-audit`只有未跟踪`.planning/mig-01-assets/`分析输出。其余11个Worktree clean。
- 主clone的4个运行代码修改（`index.html`、`src/lib.rs`、`src/render.rs`、`www/js/main.js`）与detached快照 `30e65de`（`calibration: apply frozen B1-07 WIP`）一致；额外差异主要是扩展后的`capture-origin-p0-03.cjs`和未跟踪B107校准/探针脚本。该WIP属于Rust/WASM时代的marker gate、idle pulse、单tick循环、retry和帧校准调查，不属于当前Phaser集成树。
- `b1-08`未提交修改是更早的Loading intro、0%进度与Play过渡实验；当前Phaser树已用`src/ui/`重新实现Loading/Ready/Play状态，不能直接合并旧JS。`phaser-assets-audit`未跟踪内容仅是资源审计计划输出；对应有效审计脚本已提交在worker历史并进入后续集成来源。
- 非基线dirty Worktree均保持原样，未执行clean、checkout、stash、commit或merge。它们应作为历史/WIP清单保留，不能混入 `origin/main@aa2ab7e` 的已验证行为基线。
- 阶段6B地图分块证据：`master.json`真实路径是`assets/maps/chunks/master.json`，定义28×28 tile分块、5×5网格、原图140×140和3个tileset；25个chunk均为28×28、24层、相同tileset。
- 将chunk按`index = y * 5 + x`逐层拼接后，24个图层与`final_map.json`的140×140数据逐格完全一致，错位数0；按列优先拼接则24层全部不匹配。Q-MAP-002的数据关系已经由文件直接证明。
- 运行时Network中master在导航响应后约5.194秒返回；首个chunk约5.800秒、最后一个约6.730秒，chunk响应自身跨度约0.930秒，master到最后一个chunk约1.536秒。它们均发生在采集脚本14秒Ready检查和Play点击之前；四方向输入期间没有新增chunk URL。
- 公开Bundle直接证明动态机制：分块管理器用`chunk{y * nbChunksHorizontal + x}.json`按坐标请求并缓存；GameScene按玩家周围3×3集合与相机可见范围加1块边距加载缺失chunk，并卸载目标集合外chunk。
- Q-MAP-003已关闭：`startCameraSequence`在Play前调用`preloadChunksForCameraSequence`，该方法汇总六个相机位置、视口和2块边距，通过空闲回调预载；有效坐标覆盖当前5×5地图，因此解释首屏全部25个请求。首屏预载和玩家阶段动态装卸是两个连续阶段，不矛盾。
- Human 已接受 `DEC-DOC-GOV-001` 并授权第二轮文档治理同步；决定、来源、替代关系和 Gate 历史的正式记录已迁入 `doc/v0.1/谁确认过什么（来源与决策记录）.md`。本条只记录过程，不替代台账。
- 新镜像唯一落盘位置：`sample/original-public-build/mirror/`。
- 现有下载器 `scripts/fetch-assets.mjs` 固定使用 `https://peteroravec.com/assets`，显式清单加推测路径共尝试 86 个 URL；不能单独作为完整镜像依据。
- 镜像路径规则：保留公开 URL 的 `/assets/` 后相对路径，例如 `/assets/maps/final_map.json` 保存为 `mirror/assets/maps/final_map.json`。
- 第一批应先采集已知地图 JSON 入口，再从其引用解析后续资源；不按猜测路径批量下载 tileset 或 chunk。
- 第一批 6 个地图 JSON 已于 2026-08-09 采集成功，均为 HTTP 200；镜像文件保存于 `mirror/assets/maps/`，每个文件的 URL、最终 URL、状态、类型、字节数、SHA-256、路径和时间均已写入 manifest 与 network 清单。
- 采集脚本仅使用固定白名单；重跑时相同字节内容不会重写 mirror 文件。
- 对已镜像地图 JSON 的独立复核确认：`final_map.json` 明确引用 `exterior.png`、`collisions-objects.png` 和 `tileset-particles.tsx`；`final_map_small.json` 明确引用 16 个 `exterior-small*.webp`、`collisions-objects.png` 和 `tileset-particles.tsx`。这些是下一批唯一可由地图文件直接确定的资源；chunk 文件名和 TSX 下游图片仍未知。
- 第二批已请求 19 个地图直接依赖：18 个成功；`/assets/maps/exterior.png` 返回 HTTP 404，已记录而未镜像。已镜像的 `tileset-particles.tsx` 明确引用唯一下一层资源 `/assets/maps/tileset-particles.png`。
- 原站根页 `https://peteroravec.com/` 使用 `<base href="/">`，明确引用同源 CSS `styles-DVTBSD34.css`、JS `assets/js/phaser.min.js`、`polyfills-A7F7OIKC.js`、`main-RV3Z53H4.js`、6 个 favicon，以及外部 Google Fonts CSS 与 Netlify RUM。下一步先镜像根 HTML 本身；其明确引用的资源仍需逐层白名单采集。
- 根 HTML 已镜像。对其静态资源标签的只读解析得出 39 个同源直接引用，已有 5 个地图 JSON；剩余 34 个是精确白名单，包含根 CSS、Phaser/Angular JS bundle、favicon、页面图片、卡牌和大地图。Google Fonts、Netlify RUM、普通外链均未纳入。
- 采集脚本当前重跑会重新请求既有白名单；作为历史镜像应改为默认不重取已成功镜像，只有显式 refresh 才能刷新，避免后续批次无意改变快照。
- 快照保护已实施：默认保留既有成功镜像及其元数据，新增或本地缺失路径才请求；`--refresh` 才重新请求成功项。首批 HTML 静态引用清单需要复核：初次 Recon 漏掉了 HTML 头中的 Apple touch icon 与 Microsoft tile meta 资源，不能直接作为下一批白名单。
- 已完成修正后的 HTML 资源解析：共识别 54 个同源静态资源声明，已有 5 个地图 JSON，剩余 47 个为精确白名单，包含 CSS、4 个 JS bundle、Phaser、卡牌/页面图、favicon/Apple/Microsoft 图标与大地图。外部 Google Fonts 与 Netlify RUM 仍明确排除。
- CSS 静态解析发现 12 个尚未镜像的同源 `url(...)` 资源：卡牌 foil/pattern、sparkles、monitor、cable-handler、cables3；CSS 内 data URL 为内嵌内容，不应作为外部采集文件。
- 第六批 CSS 依赖中 10 个成功；`card5_foil.webp` 与 `cables3.png` 均为 HTTP 404，已记录未镜像。加上既有 `exterior.png`，当前已有 3 个已确认 unavailable。快照保护还应覆盖已确认失败项，避免每个后续批次重复请求同一 404，除非使用 `--refresh`。
- 应用 JS 静态解析发现 14 个尚未镜像的精确资源：2 个动态 import chunk、hover-tilt 脚本、4 个 Under the Hood 图片、卡牌 2/3 foil-pattern、3 个作品图和 mini-map。其余已分析的 polyfills/静态 chunk 无新增；动态 chunk 的内部依赖须在镜像后再单独分析。
- 动态 gameplay chunk 的静态 Loader 常量解析表明存在约 140 个未镜像的地图、NPC、车辆、特效和精灵资源；Recon 报告的总数、缺失数与分类列表数量不一致，不能在未做机器化去重核验前直接据此进行大批量采集。
- 主助手以独立正则与 manifest 复核：bundle 中有 152 个唯一 `/assets/` 字面量，其中 142 个未镜像；其中 `/assets/maps/` 是动态模板前缀而非完整资源，排除后得到 141 个精确静态 pathname。这与 Recon 的“141 未采集”结论一致，可作为下一批白名单来源。
- 运行时 Network 与 manifest 对比得到 28 个真实未镜像同源响应：25 个 `maps/chunks/chunk0.json` 至 `chunk24.json`、2 个 UI WebP（`map-holder-mini3.webp`、`cable-handler2.webp`）可安全补采；唯一其余项是 Netlify RUM 脚本，属于分析/遥测，不纳入参考镜像。
- 第九批已采集运行时验证的 27 项，全部 HTTP 200；当前镜像 265 个成功文件、3 个明确 404。主助手独立确认运行时记录中的 224 个同源 2xx 非 Netlify URL 均已镜像，缺失为 0；Verifier 对该集合报出 265 的计数错误，但其余文件/哈希/清单一致性检查通过。
- 阶段6C SYS-CHUNK直接证据复核：master证明5×5、每块28×28、16px和行优先文件公式；Bundle证明玩家3×3与相机范围+1块边距合并为目标集合、场景和manager两层缓存、场景卸载清除Tilemap，以及Play前相机序列空闲预载。并发请求去重、取消、重试、场景销毁清理和多视口预载边界仍为 UNKNOWN。manager虽定义缓存淘汰方法，但已检查Bundle未发现调用方，不能推定其动态缓存淘汰语义。独立复核曾发现未清除图层数误记为11，已更正为13。
- Human 已明确接受轻量任务接力方案：节点与工作项分离；只持久化正式工作项；普通工作项使用单次原子提交；正式代码、技术栈或范围扩大使用一页授权包与 Human Gate；完整 P/A 只在破坏性、真实数据、并行写入或跨仓库高风险事务中启用。
- 首次真实试点最初限定为 `WI-SYS-CHUNK-CORE-001` 的 proposed 授权包与 `awaiting-authorization` 任务卡；后续 Human 已通过 `DEC-SYS-CHUNK-CORE-001` 授权最小 TypeScript 环境和确定性 CORE，Phaser 集成继续未授权。
- CORE 结果提交 `f04568f953821e8cc56c33a694171ddab759051f` 只包含批准路径：master 契约、行优先索引/文件名、世界坐标边界、玩家3×3、相机 scroll/zoom +1块边距和确定性目标并集；3文件26项测试、strict typecheck、官方源 audit 0漏洞、治理 sync/pilot 和最终独立复核均通过。
- 独立复核发现的唯一 LOW 是内部坐标合并 helper 被公开导出；Main 已在结果提交前改为模块私有，并用重叠玩家/相机目标测试继续证明去重和稳定顺序。没有出现第二个独立复用场景，复用观察保持“未发现”。
- 首次真实接力暴露治理缺口：工作项完成且 `preauthorized-next-work-item: none` 时，当前机器合同不允许 `current-work-item: none` 或终态 `completed`，导致无法同时做到“关闭索引已完成”和“当前状态不漂移”。已登记 `BLK-WI-CLOSURE-001`，禁止通过伪造 active/completed 状态绕过。
- Human 已接受 `DEC-WORK-RELAY-002`：正式工作项允许为零或一个；没有预授权下一项时正常关闭并回到既有 P0、节点、依赖、未知和差距来源，不制造虚假选择 WI。CORE 已写入关闭索引，当前正式工作项为 `none`，新的正式代码继续未授权。
- 下一项候选复核确认：SYS-WORLD与SYS-LAYER边界是accepted P0顺序的下一项，并同时解除SYS-ASSET和SYS-CHUNK Phaser集成的前置未知；Human已接受`WI-SYS-WORLD-LAYER-DESIGN-001`，范围仅为调查、设计候选和验证计划。
- 激活level-1时发现治理检查器把所有非空工作项的`scope-ref`都强制解释为实施授权包，并要求`GATE-*`，与流程速查中level-1简短任务卡、无代码Gate的规则冲突。采用最小修正：level-1使用`work-item-task-card`和`none / not-applicable`，level-2继续使用实施授权包与Human Gate。
- WI-SYS-WORLD-LAYER-DESIGN-001第一步确定性提取：25个chunk的24层名称、顺序、tilelayer类型、28x28尺寸和784项data完全一致；按5x5行主序拼接后与final_map的24层、470400格逐格差异为0。24层依次为layer1~10、cars、4个roof、4个bridge wall、walls、particles、particles2、particles3、footsteps。
- 地图文件直接证明：layer1~10和walls为visible，cars、roof、bridge、particles与footsteps为hidden；仅walls opacity为0.84，其余为1；各层没有properties或offset字段。因此文件本身不能单独证明隐藏层的运行时语义。
- Recon初步定位公开Bundle：由master originalWidth/originalHeight建立140x140空Tilemap并映射exterior、collisions-objects、tileset-particles；发布代码同时存在普通chunk写入路径和USE_OPTIMIZED_TILESETS相关整图small路径，实际运行选路仍需Main抽查或行为证据。
- Recon初步定位图层处理：基础层、walls、cars、roof、bridge有明确创建/depth/碰撞或动态行为；particles/particles2进入优化处理清单，particles3未进入；footsteps tilelayer未进入已定位写入/清除清单，另有footsteps-layer网格驱动脚印，二者不能直接等同。
- Recon初步定位卸载边界：已定位unloadChunk只清layer1~10和walls共11层并移除loadedChunks标记；其余13层的卸载、完整销毁、在途取消、重试与失败回滚仍是UNKNOWN。上述Bundle语义需Main对关键锚点做抽查后再进入正式调查记录。
- Main关键锚点抽查确认：发布类字段`USE_OPTIMIZED_TILESETS=!0`且当前只定位到该赋值；优化loadChunk路径使用140x140的final_map_small并处理22层，明确排除particles3和footsteps。初始创建对layer6~10使用1500~1900 depth，而优化函数仅在层缺失时使用1100~1500公式，两套公式并存，不能合并成单一原站规则。
- Main抽查确认：初始空世界创建layer1~10、4个roof、walls和bridge1上下层；优化路径可补建cars、particles/2和bridge2。unloadChunk仍只清11层。已据此建立SYS-WORLD和SYS-LAYER两份`main-definition: false`调查记录，节点状态不变。
- 第二轮确定性对照证明：`footsteps-layer.json`的140x140布尔grid与final_map的footsteps层368个GID=69345位置逐格完全相等；两者是同一位置数据的不同编码，但运行时Bundle只读取独立grid，尚未定位tilelayer消费者。
- final_map与final_map_small的particles、particles2、particles3三层逐格完全相同；particle-trajectories有88个多边形region，只有water/water2/protesters_rising三类全局tileCount与单一GID计数吻合，其余类型无法由纯数据键、bbox或多边形精确复现，因此不能宣称轨迹JSON由tilelayer机械生成。
- 第二轮Recon补查：应用Bundle只有3个putTilesAt调用点，特殊13层没有unloadChunk之外的tile清空；普通分支虽然遍历chunk的24层，但`o=["layer1"]`守卫使其实际只写layer1；默认优化分支固定22层并排除particles3/footsteps。GameScene未定位统一Tilemap destroy或idle callback取消，完整teardown仍UNKNOWN。
- 玩家常态depth约为`500 + (y+24)*0.1`，桥上可强制1650，怪物抓取和龙卷风另有覆盖；因此Q-LAYER-001的玩家遮挡不能只靠静态图层depth关闭，仍需核对动态时序和必要的既有/新增行为证据。
- 当前设计候选建议第一版以master+chunk作为唯一运行数据来源，final_map/final_map_small只作Oracle；SYS-WORLD拥有Tilemap、图层实例和已渲染集合，SYS-LAYER提供24层完整策略，SYS-CHUNK继续拥有目标/请求状态。该设计避免复制发布Bundle整图优化与chunk动态写入并存的状态分叉，代价是需要行为验证证明视觉一致。
- 图层设计候选将基础视觉、隐藏碰撞、动态roof/bridge和marker数据分开；所有chunk来源数据都要求对称remove，不照搬特殊13层未清除。footsteps建议以chunk tilelayer为运行来源、外部grid作Oracle；particles3在消费者未知时保留marker并明确未完成。
- Human已接受SYS-WORLD与SYS-LAYER当前详细设计，并授权当前项关闭后的5场景最小视觉补证。设计接受不关闭Q-LAYER-001，也不授权正式代码；补证只允许公开原站、白名单截图/JSON和必要采集脚本。
- 原站1280x720运行补证：玩家从clear tile(68,74)进入layer8 tile(68,75)时player depth 621.6→623.2、layer8 depth 1700，截图显示上层灌木遮住玩家下半身；离开恢复。
- factory roof运行补证：进入(360,904)后roof_factory/roof_factory2 alpha由1变0，离开恢复1；concert两层保持1。
- bridge1运行补证：方向键从左入口进入后down wall碰撞tile 14→0、up wall 0→82、玩家depth变1650；从右出口离开后down wall和玩家常态depth恢复。
- particles3位置运行补证：优化运行Tilemap没有particles3层；在86格marker范围内玩家同时落入protesters_rising(tileCount=86)和crowd_up region，截图有抗议人群。marker到trajectory消费者的直接链路仍UNKNOWN。
- footsteps运行补证：优化运行Tilemap没有footsteps层；玩家沿独立grid对应路径移动后active footprint sprites由0增至5，depth 450。5场景均获得可定位运行证据。
- 总文档迁移预检发现安全基线`03fe5ac`上存在一处非本轮产生的能力清单表格格式化差异；Human确认后已将其作为独立提交`a2e303b`保存，内容语义未变化，迁移工作区恢复clean。
- `00-迁移结合路线.md`的SHA-256为`20bde27e97b7ec2bda2753bf1f31d33fa0baf186da43756661001de1d97825cd`。Human授权执行第0至第7步；来源目录删除和路线第8步仍未授权。
- 现有工作项检查合同要求所有WI引用游戏节点，但稳定流程允许纯治理任务。迁移准备采用最小合同：仅`work-item-type: governance`可使用`node-refs: not-applicable`，正式代码level-2 Gate保持不变。
- 路线原文认为从迁入后的`05-素材/`继续使用`../虚拟校园项目/`可以到达兄弟目录；实际解析会落到仓库内部。正确相对路径是`../../虚拟校园项目/`，`04-内容层/`中的同类历史链接也需要两级上行。
- 26份指定人话文档已从来源复制入仓库并逐文件SHA-256核对，差异0；来源目录仍保留28份Markdown（含来源README和迁移路线），未执行清理。
- clean基线`cda9817`独立复核确认：迁入范围旧路径残留0、11个关键物理路径存在、`src/tests`自安全基线零变化、`sample`只改治理检查器、`doc/v0.1`只改迁移治理记录和任务状态；路线第8步确未执行。
- Human已独立授权执行第8步和普通GitHub推送。清理前旧目录28份Markdown中的26份已有迁入Git对象；另两份原迁移路线和迁移前README已按原字节归档进`migration-history/`，因此清理重复目录不会丢失不可恢复内容。
- 第8步执行后`D:\复盘\虚拟校园项目·总文档`仅剩一份跳转README、子目录0，跳转页SHA-256为`eaac4b572e2eb6a460b0e18dea81de0ba62a11e12fb792919540a418766bbeba`；旧目录本身未删除。
- cleanup clean基线`e037559`独立复核确认8项全部PASS：26份迁入内容零变化、两份原件哈希正确、`src/tests/sample`零变化、任务状态一致、Git对象正常；推送前无blocking/high/medium缺陷。
- 首次push暴露远端PR merge拓扑但无内容差异；普通无内容merge后本地`b231904`与`origin/main`一致。全程未force、未改写历史、未覆盖远端内容。
- Human提供`执行层迁移路线.md`并明确要求继续迁移；原件SHA-256为`258edd124fd0d4f0e8675f0f039197f4a427a01e9a1c6d4ad3c82e572582ef38`，安全基线`1fabc8a`与远端一致。当前`doc/v0.1`有47份Markdown，理解层有16张系统卡，现执行层只有README。
- 路线内部“检查器归档”与“sample一个字节不碰/最终diff为空”冲突；按铁律和验收优先，旧检查器原文件保持不动，只在历史区保存快照。路线所称“还差5张截图”是过期描述，权威状态是5场景证据已完成并等待Human审查；迁移只暂停和恢复审查。
- 路线第1步已生成与理解层路径完全一致的16张执行卡：SYS-WORLD/SYS-LAYER/SYS-CHUNK从各自正式设计、调查记录和验证计划抽取为7格`designed`卡，其余13张为统一空槽。三个保留口子继续为TBD，CHUNK只声明确定性CORE代码，不误报完整实现。
- 路线第2步将16系统两条状态线、主要行为/差距和7 open+4 closed未知项收敛到`03-执行层/00-总账.md`；总账明确不复制动态工作项。操作手册第一页写入6条规矩和6步循环，并直接链接16张当前执行卡。
- 路线第3步将11项仍生效Human决定、8项Gate和已替代/完成签字历史抽到根`决策记录.md`；动态current状态仍只认`task_plan.md`。根README新增Phase 1目标、纳入/排除、证据、复用和逐项代码授权边界，不提前切换第6步执行入口。
- 路线第4步把`doc/v0.1`除当前视觉任务卡外的46份Markdown按原相对结构移动到`migration-history/doc-v0.1/`，逐文件SHA-256一致。旧检查器原文件保持不动，历史快照SHA-256同为`97d6ac9a320a6c27c685a04fca7c3c8e477239abc6cb1d46fc0939c0a832f83c`。
- 路线第5步建立`doc/v0.1/README.md`兼容跳转；旧目录当前只有README和视觉任务卡两个文件，分别指向现执行层/总账/决策/动态状态/历史和当前Human审查任务。
- 路线第6步把AI启动、当前精确知识、Human决定、稳定流程和接力说明统一切到`03-执行层/`、根`决策记录.md`与`task_plan.md`；API 契约表、术语表和三张有肉卡的源路径也已避免指向已迁出的旧位置。
- clean结果`293cbeb`的独立复核确认路线10类检查全部PASS；两份归档治理文档相对安全起点的内容差异来自第0步合法登记本次SRC/DEC，归档移动本身为R100无损。当前分支未推送是任务卡明确边界，不是缺陷。

## 错误记录（原 task_plan.md 错误记录表）

| 错误 | 处理 |
|---|---|
| Node/Python 内联命令含 shell 反引号导致解析失败 | 改用不含反引号的 Python 正则；成功提取 bundle 静态路径。 |
| Verifier 报告运行时同源 2xx 数为 265，与主助手独立计数 224 不一致 | 不采纳 Verifier 的该计数；主助手确认 224 个运行时 URL 均已镜像，coverage 缺失仍为 0。 |
| Windows 拒绝直接重命名新项目根目录 | 自动回滚后改用复制、299文件逐项 SHA-256 校验、再删除原目录；迁移成功。 |
| 复用与执行约束只停留在对话，未及时写入 AI 必读文档 | 停止继续口头扩展；将有效原则、Human Gate、复用观察和按需门禁写入项目入口与模板。 |
| 使用含反斜杠的内联 `rg` 正则检查路径时转义失败 | 改用 `rg -F` 分别执行固定字符串检查；后续路径检查不拼接复杂正则。 |
| GitHub 仓库创建后首次两次 HTTPS 推送连接失败 | 保留已创建的 Private 仓库和正确 origin；Human 调整网络后重试成功，本地与远程提交一致。 |
| 阶段1浏览器采集后的Python摘要脚本因Windows临时目录路径解析错误触发 `StopIteration` | 主采集已成功；不重复采集，改为定位既有输出并用明确的Windows绝对路径解析。 |
| 首次Sandbox相机右边界断言在Scene首帧读取到未稳定的 midpoint 2240 | 测试读取时机早于resize与平滑跟随稳定；修改临时测试，在首次指标出现后等待并读取稳定帧，再复核真实边界。 |
| Sandbox浏览器检查发现 `/favicon.ico` 返回404，严格零失败断言中止 | 记录为现有开发页非功能性缺口；测试仅豁免该已知favicon请求，其他HTTP失败仍阻塞。 |
| 独立验证器超出只读要求，尝试重跑 `npm ci`，因遗留esbuild进程锁文件失败，随后用 `npm install` 恢复node_modules | tracked文件和lockfile均未变化；Main清理阶段1遗留服务进程并再次确认旧worktree clean。 |
| 首次进程清理脚本的匹配条件包含仓库路径和端口文本，误匹配并终止了执行清理的shell自身 | 改为根据netstat确认唯一残留监听PID 16460后精确终止；8197/8198均无监听，旧worktree保持clean。 |
| 阶段6B按旧工程路径读取 `assets/maps/master.json` 返回ENOENT | 不猜测路径；从manifest和镜像磁盘定位真实公开URL路径后再读取。 |
| 打印minified Bundle片段时Windows控制台GBK无法编码符号并中止 | 设置Python UTF-8输出并只截取目标类片段，不重复原命令。 |
| CORE 实现时首次打印 chunk manager Bundle 片段再次触发 GBK 编码错误 | 未重复原命令；立即设置 `PYTHONIOENCODING=utf-8` 后成功定位公式。 |
| `npm audit` 使用本机 npmmirror 返回未实现的审计端点 | 改用 `--registry=https://registry.npmjs.org`；审计完成，0 vulnerabilities。 |
| 轻量接力首次同步检查发现3个旧章节锚点和1个合并路径表格单元无法解析 | 将3处链接同步到历史授权新标题；把 `findings.md` 与 `progress.md`（现合并为 `工作日志.md`）拆为两行受影响文件；删除 `py_compile` 生成的未跟踪缓存，后续用内存编译检查。 |
| level-1激活首次治理检查把`findings.md`与`progress.md`（现合并为 `工作日志.md`）合并单元解析成不存在路径 | 将两个受影响文件拆成独立表格行；不修改路径解析器或降低检查标准。 |
| Main首次脚印grid抽查脚本因集合推导式变量遮蔽误报与tilelayer不一致 | 不采纳初次输出；改用明确`(y,x)`坐标集合审计，368个位置直接交集368、双方独有0，确认逐格一致。 |
| Recon将普通chunk分支的全层遍历误报为可写全部24层 | 独立verifier检查守卫`o=["layer1"]`后发现实际只写layer1；Main核对Bundle并同步修正调查记录、设计候选、验证计划和findings（现合并为 `工作日志.md`）。 |
| 正式设计接受后首次sync/pilot提示`level-1 selection decision does not name current work item` | 在`DEC-SYS-WORLD-LAYER-DESIGN-001`适用范围显式加入`WI-SYS-WORLD-LAYER-DESIGN-001`；不降低检查标准。 |
| 视觉补证首次激活检查提示工作项类型、节点范围和任务卡状态不匹配 | 使用检查合同允许的`investigation`类型；将任务卡`node-refs`改为与task_plan一致的标量并将顶层status同步为active；不修改检查器。 |
| Chrome DevTools MCP导航和连接连续超时 | 未产生采集结果；改用本机既有Puppeteer和系统Chrome访问同一公开URL，不增加项目依赖或扩大范围。 |
| 首轮视觉脚本直接warp进bridge区未触发方向切换，且脚印统计误把95个inactive对象池成员当现有脚印 | 不采纳首轮对应结论；改为从入口/出口外用方向键自然跨入，并只统计active footprint。重跑后bridge上下状态均观察到，active脚印由0增至5。 |
| 独立复核指出行为基准和证据差距回写未逐字列入视觉任务卡白名单 | 两项都是AGENTS.md第8节要求的强制闭环；将其显式补入任务卡允许路径，避免依赖隐含协议解释，不扩大采集或实现范围。 |
| 迁移关闭索引使用无路径的README双链时治理检查发现两个同名候选 | 改用明确的Markdown相对链接指向根`README.md`；错误记录不再原样嵌入会被检查器解析的双链语法。 |
| 第8步预检将两份原字节历史来源放入`doc/v0.1`后触发正式文档命名和无效双链检查，且插入决定时重复了一次迁移决定标题 | 不修改历史原件字节；将它们移到仓库根`migration-history/`并更新权威路径，同时删除重复标题。治理文档继续保持正式命名合同。 |
| 清理结果首次暂存命令同时列出`git mv`后的新旧文件路径，旧路径已不存在导致pathspec失败 | 未形成提交或丢失变更；改为对`doc/v0.1`和`migration-history`目录执行`git add -A`，由Git记录重命名。 |
| 首次普通push被拒绝，远端main多出PR #1的merge提交`971d2a3` | 停止推送且未使用force；fetch后确认该远端提交相对共同基线`03fe5ac`内容差异0、tree完全相同，使用无内容merge提交`2824d78`合并远端拓扑后再普通推送。 |
| 执行层迁移首次激活检查发现插入位置重复了旧清理决定标题，且accepted决定不能把尚未执行的未来路径标为pending | 删除重复标题；受影响清单按每步实际变更追加并标updated，完整未来范围继续由任务卡约束，不伪报同步完成。 |
| 首次打印16张理解卡标题时Windows GBK无法编码标题中的星号符号 | 未修改文件；设置`PYTHONIOENCODING=utf-8`后重新执行只读清单。 |
| 第1步受影响清单把多个目录写成一个“从A至B”代码路径，治理检查器按单一路径解析而报缺失 | 改为登记实际存在的共同目录`03-执行层/`，具体16卡由步骤验收脚本逐项核对。 |
| 第7步首次当前文档链接脚本递归进入`node_modules`并把依赖包README的发布链接误报为项目断链 | 未修改依赖；链接检查明确排除`node_modules`、`.git`和历史归档，只验证项目当前Markdown。 |
| 关闭提交首次暂存同时列出已被`git mv`移走的源路线/任务卡旧路径，pathspec不存在导致失败 | 未形成提交或丢失变更；改用`git add -u -- .`记录全部已跟踪修改/删除，再显式加入`migration-history`新路径。 |

## 2026-08-18 全盘审计落盘记录

> 本记录只保存审计结果，不代表修复、授权或新的项目决定。项目动态状态仍以`task_plan.md`为准。

### 范围与边界

- **FACT**：已按项目`AGENTS.md`启动协议检查当前入口、`03-执行层/`、根决策与动态状态，并检查`package.json`、Vite/HTML、`game/`、`src/`、`tests/`、`scripts/`、`public/`和Git工作树。
- **FACT**：本轮没有修改`src/`、`game/`、`sample/`、`task_plan.md`或`决策记录.md`，没有读取旧项目、历史归档、Evidence或远端PR。
- **INFERRED**：以下影响判断来自代码和文档结构审查，尚未等同于浏览器或生产环境证明。
- **UNKNOWN**：未重新进行浏览器/移动端验收，未验证远端PR和隔离worktree事实。

### P1 高优先级

1. **入口文档低报正式CORE实现状态**
   - **FACT**：`AGENTS.md:12`、`README.md:73`、`01-理解层/00-进度总览.md:25`仍把`src/`描述为仅有SYS-CHUNK或仅1/16写码。
   - **FACT**：`03-执行层/00-总账.md:24-31`和`task_plan.md:166-173`已记录8个CORE，实际存在`src/{asset,camera,chunk,input,layer,move,player,world}`及对应测试。
   - **INFERRED**：后续执行者可能误判已授权/已实现范围。
   - **建议**：后续单独建立治理文档同步工作项；本轮不修复。

2. **玩家深度公式存在权威冲突**
   - **FACT**：`02-接口层/API契约表.md:47`写`500+y*0.1`并标为已定死。
   - **FACT**：`03-执行层/01-地图线/03-图层与遮挡.md:74`及`决策记录.md:48`采用`500+(y+24)*0.1`，并说明旧公式待对账；SYS-PLAYER卡仍保留旧公式。
   - **INFERRED**：实现、接口和验收可能使用不同深度结果。
   - **建议**：需要Human确认唯一公式后再同步；本轮不改。

3. **运行资源不受版本控制且不在构建链生成**
   - **FACT**：`.gitignore:3`忽略整个`public/`；`git ls-files`没有列出必要运行资源。
   - **FACT**：`game/CampusScene.ts:40-43`和`index.html:44`依赖`/maps/*`、`/sprites/*`、`/vendor/phaser.min.js`；`package.json:8-14`没有资源准备步骤。
   - **INFERRED**：干净克隆构建/部署无法复现本机运行资源，可能出现404和启动失败。
   - **FACT**：`scripts/sanitize-map.mjs:16-18`依赖固定本地输入并生成被忽略的`public/maps/final_map.json`，未接入构建命令。
   - **建议**：建立资源归属和构建可复现性工作项；本轮不改。

4. **自动检查没有覆盖浏览器入口**
   - **FACT**：`tsconfig.json:14`只包含`src/**/*.ts`和`tests/**/*.ts`，不包含`game/`、`vite.config.ts`。
   - **FACT**：`game/phaser.ts:3`使用`any`读取全局Phaser，`game/CampusScene.ts`没有对应自动化测试。
   - **INFERRED**：typecheck/test/build全绿不能证明实际Phaser生命周期、资源解析和浏览器启动成功。
   - **建议**：增加入口类型检查或浏览器Smoke Gate；本轮不改。

### P2 中优先级

1. **SYS-WORLD测试计数漂移**
   - **FACT**：总账与task_plan记录29项，`决策记录.md`记录30项；实测`tests/world`为30 passed。
   - **建议**：后续文档同步时统一为可复现结果；本轮不改。

2. **task-todos形成潜在第二状态入口**
   - **FACT**：`task-todos/2026-08-18-sync-local-and-review-prs.md:4`标记`in-progress`，而唯一动态权威`task_plan.md:3`当前是`WI-VERIFY-CURRENT-WORK-ITEM-001`。
   - **INFERRED**：若该文件被当作正式任务，可能出现两个并行进行中工作项。
   - **建议**：明确其为非权威交接备忘，或纳入合法工作项索引；本轮不改。

3. **生产诊断面板泄露内部错误信息且无上限**
   - **FACT**：`index.html:20-41`把错误消息、文件名、行号、stack及所有`console.warn/error`写入可见`#diag`，日志数组无长度上限。
   - **INFERRED**：部署后可能暴露源码路径/依赖信息，持续错误也可能造成页面内存增长；当前使用`textContent`，未发现由此直接产生的HTML注入。
   - **建议**：生产环境关闭或限制诊断面板，并限制日志数量；本轮不改。

4. **World原子回滚依赖hook不发生部分写入**
   - **FACT**：`src/world/world.ts:100-113`仅在`writeLayer`正常返回后登记层；若hook已写入后抛错，该层不会进入`written`，后续无法清除。`removeChunk:124-140`存在对称风险。
   - **INFERRED**：未来真实Tilemap hook部分失败时可能留下未登记tile。
   - **建议**：明确hook事务契约或实现可检测/可补偿写入；本轮不改。

### P3 低优先级

- **FACT**：`task-todos/2026-08-18-sync-local-and-review-prs.md:5,8`frontmatter重复`version`键，不同YAML解析器可能取不同值。

### 已执行只读验证

- `python scripts/check-state-consistency.py`：PASS。
- 当前Markdown链接检查：broken=0（43份范围内文档）。
- `npm run typecheck`：PASS。
- `npm test`：25个测试文件、133项PASS。
- `npm run build`：PASS。
- `tests/world`：30 passed。
- `git diff --check`：PASS。

### 审计状态

- `proposed`：审计发现已形成。
- `persisted`：本节已追加到本日志；详细执行边界见`C:\Users\inertnet\.pi\plans\plan-2026-08-18-audit-persistence.md`。
- `verified`：已完成工作树、受保护路径、状态一致性和差异检查；检查通过。
- **尚未解决**：上述P1/P2/P3均未修复；玩家深度公式、运行资源可复现性和浏览器入口验证仍需后续Human授权工作项。

## 2026-08-18 文档总控修复

- **accepted**：Human 以“ok。开始吧”接受先恢复总控台、再按工作项修复的顺序。
- **persisted**：激活 `WI-DOC-CONTROL-REPAIR-001`，授权 `DEC-DOC-CONTROL-REPAIR-001`；更新 `计划表.md`、`01-理解层/00-进度总览.md`、SYS-PLAYER理解/执行卡、API契约表、SYS-WORLD总账/执行卡，并统一本次状态口径。
- **事实修正**：明确 8 个确定性 CORE 不等于 8 个完整系统；完整系统仍为 0 个最终 `implemented/verified`；SYS-WORLD 测试数统一为 30。
- **公式修正**：保留原站 FACT `500+y*0.1`，同时明确当前重构 DECISION 以 SYS-LAYER 的 `500+(y+24)*0.1` 为准，避免把原站事实和重构决定混为一谈。
- **verified**：`python scripts/check-state-consistency.py` PASS；`git diff --check` PASS；`src/`、`game/`、`public/`、`sample/`、`tests/`没有本工作项新增修改。
- **尚未解决**：资源可复现、浏览器启动/视觉验证、诊断面板和World部分失败回滚尚未开始；下一项需Human重新选择并授权。
- **验证过程错误**：首轮Markdown链接正则无法处理文件名中的括号，误报2条断链；第二版内联Python转义反斜杠语法错误；未采纳两次结果，改用括号平衡扫描后`broken=0`。
- **Git提交**：文档总控修复结果已提交为 `c40f6df`；未暂存或提交未跟踪的 `task-todos/`。
- **状态检查错误**：`current-work-item: none` 时顶部下一步误写候选 WI 编号，触发检查器把候选误判为当前项；已改为只在“近期候选”表保留编号。
- **工作项切换**：文档总控结果提交 `c40f6df` 后关闭 `WI-DOC-CONTROL-REPAIR-001`，激活 `WI-RESOURCE-REPRO-001` 做只读资源可复现性调查；资源实现仍未授权。

## 2026-08-18 资源可复现性调查

### 已确认事实

- **FACT**：`game/CampusScene.ts` 直接依赖 `/maps/final_map.json`、`/maps/exterior-final.webp`、`/maps/collisions-objects.png`、`/sprites/player.webp`；`index.html` 直接依赖 `/vendor/phaser.min.js`。
- **FACT**：当前 `public/` 只有 5 个本地文件：`final_map.json` 1,102,500 bytes、`exterior-final.webp` 517,502 bytes、`collisions-objects.png` 3,941 bytes、`player.webp` 11,252 bytes、`phaser.min.js` 906,004 bytes；`git ls-files public/*` 为空，`.gitignore:3` 忽略整个 `public/`。
- **FACT**：上述 5 个文件都能在 `sample/original-public-build/mirror/assets/` 找到同名来源，字节大小一致；因此当前运行依赖的是本机手工/临时生成的派生目录，不是Git可复现输入。
- **FACT**：`scripts/sanitize-map.mjs` 只从 sample 的 `final_map.json` 生成 `public/maps/final_map.json`，没有复制其他4个运行文件，也没有接入 `package.json` 的 `build/dev/preview` 命令。
- **FACT**：公开原始地图包含外部 tileset `tileset-particles.tsx`，其图片来源为 `tileset-particles.png`；原始地图有 3,373 个 GID ≥ 69355 的粒子相关 tile。当前 `public/maps/final_map.json` 已移除外部 tileset，且高 GID 数为 0。
- **FACT**：当前 Phaser 雏形只创建 layer1–10 的视觉层，不创建粒子层；所以 sanitize-map 的做法是一个“当前雏形范围的派生决定”，不是完整原站资源解决方案。

### 推断与风险

- **INFERRED**：干净克隆后 `public/` 为空，`npm run build` 即使通过，浏览器仍会因地图、人物或 Phaser 资源缺失而无法启动。
- **INFERRED**：直接把外部 tileset 静默删除会牺牲粒子数据；如果不登记 DECISION，后续会把“没有粒子”误报成原站事实或完整实现。

### 可选实现方案

- **方案 A：可复现生成（推荐最小方案）**：保留 sample 作为公开来源，新增受控的 runtime asset prepare 步骤，复制4个必要资源并生成 sanitized map；把粒子省略明确登记为当前可玩雏形 DECISION，并把资源检查接入 build/dev/preview。优点是避免重复提交二进制；代价是构建依赖仓库内 sample 路径，且不包含粒子完整运行。
- **方案 B：运行资源纳入Git**：将当前需要的运行资源和派生地图纳入版本控制，并修改 `.gitignore` 例外。优点是干净克隆直接可运行；代价是二进制重复、派生文件可能与 sample 漂移，仍需记录粒子省略决定。
- **方案 C：完整外部 tileset 管线**：保留/内联 `tileset-particles.tsx` 与 png，并让 Phaser 正确解析外部 tileset，再决定粒子层渲染。优点是更接近原始地图；代价是范围扩大到 Tilemap/粒子系统，不适合作为当前最小资源修复。

### 当前建议

先采用**方案 A**完成干净克隆和可玩雏形的可复现启动，再把粒子外部 tileset 作为独立 SYS-FX/SYS-LAYER 工作项；不在资源修复中偷偷删除或伪造粒子行为。方案 A 落地前仍需Human确认这是当前重构决定。

### 调查验证

- 必需路径与入口引用清单：PASS。
- public 与 sample 同名来源清单：PASS。
- 原始/派生地图 tileset 与高 GID 对比：PASS。
- `git check-ignore` 确认 public 被忽略：PASS。
- 本调查未修改 `src/`、`game/`、`public/`、`sample/`。

### 调查工作项关闭

- **persisted**：调查记录已进入本日志，结果提交为 `f8a9014`。
- **verified**：资源清单、外部 tileset 对比、状态一致性和 `git diff --check` 均通过。
- **尚未解决**：方案 A/B/C 尚未由Human选择；没有任何资源实现、构建脚本或地图修复被授权。

## 2026-08-18 资源可复现性实现授权

- **accepted**：Human 选择方案 `A`：从仓库内 sample 公开镜像生成运行资源，不把派生 `public/` 当作源码提交。
- **persisted**：激活 `WI-RESOURCE-IMPLEMENT-001`，授权 `DEC-RESOURCE-IMPLEMENT-001`；允许修改 `scripts/`、`package.json` 和被忽略的派生 `public/`。
- **边界**：本次不改 `src/`、`game/`、`sample/`，不纳入粒子 tileset，不修浏览器视口和完整粒子渲染。

### 资源实现工作项关闭

- **persisted**：资源准备脚本、检查脚本和 package 生命周期已提交为 `6815a6f`。
- **verified**：本地与干净检出均完成资源准备、检查和 build；typecheck 与 133 项测试通过。
- **尚未解决**：浏览器真实启动、视口缩放、粒子完整渲染、运行时诊断和World回滚仍需后续工作项。

## 2026-08-18 浏览器启动验证授权

- **accepted**：Human 以“继续”授权 `WI-BROWSER-STARTUP-001`。
- **persisted**：允许修改 `game/`、`tsconfig.json`、必要验证脚本/命令入口；不修改 `src/`、`public/`、`sample/`，不自动签署视觉 Gate。

## 2026-08-18 资源可复现性实现

- **persisted**：新增 `scripts/prepare-runtime-assets.mjs` 和 `scripts/check-runtime-assets.mjs`；`package.json` 增加 `prepare:runtime`、`check:runtime`，并接入 `dev/build/preview` 生命周期。
- **实现内容**：从版本化的 `sample` 公开镜像复制地图纹理、碰撞图、玩家图和 Phaser；调用既有 sanitizer 生成 24 层 sanitized `final_map.json`；粒子外部 tileset 和 3,373 个高 GID 保持为后续独立范围。
- **verified**：资源准备 PASS；资源检查 PASS；typecheck PASS；25 个测试文件、133 项测试 PASS；build PASS。
- **clean checkout verified**：从提交 `6815a6f` 建立临时干净 worktree，初始无 `public/`；`npm ci --ignore-scripts --prefer-offline`、资源准备、资源检查和 build 全部 PASS；临时worktree已移除。
- **验证过程错误**：第一次在仓库外直接执行 `npm run prepare:runtime`，因当前目录不是项目根而报 Missing script；改用 `npm --prefix D:/复盘/虚拟校园项目重构 ...` 后验证通过。
- **尚未解决**：浏览器真实启动、视口缩放、粒子完整渲染和运行时诊断/回滚问题仍未处理。

## 2026-08-18 浏览器启动自动验证

- **persisted**：`tsconfig.json` 纳入 `game/**/*.ts` 和显式 `game/phaser.d.ts`；新增最小 Phaser 3 全局类型声明，结果提交 `ffa97d0`。
- **persisted**：新增 `scripts/browser-smoke.mjs` 和 `npm run browser:smoke`，结果提交 `7c5a738`；脚本连接 Chrome DevTools Protocol，不启动或关闭用户日常浏览器。
- **verified**：真实 Vite 页面 Smoke 通过：页面标题为“虚拟校园 · 可玩雏形”；`window.Phaser=true`；canvas 数量 `1`，尺寸 `764x485`；地图、外观图、碰撞图、玩家图和 Phaser vendor 响应均为 HTTP 200；未捕获 JavaScript 异常；诊断面板为空且隐藏；Tilemap 已成功创建并渲染。
- **verified**：截图已生成到 `C:/tmp/campus-smoke-persisted.png`，可见校园地图、火车和玩家；资源检查、typecheck、133项测试和 build 均通过。
- **已知非阻塞警告**：页面请求 `/favicon.ico` 返回 404；它不是 `CampusScene` 的运行依赖，当前 Smoke 将其单独忽略，但应作为后续入口清理项记录。
- **Human Gate**：自动验证通过不等于视觉一致性通过；当前状态为 `visual/awaiting-human`。视口缩放是否符合原站、画面是否接受，需要Human查看截图后决定。

## 2026-08-18 正式多人协作触发

- **accepted**：Human 明确说明已引入第二位协作者并开始协作。
- **采用范围**：吸收 PR #2 的分支/worktree、main保护、变更前后状态检查、预览验证、Human确认、push/PR边界和建PR后停止规则。
- **不采用**：不原样合并 PR #2；不把 PR #2/PR #3 的 `task-todos/` 当作 `task_plan.md` 的动态状态源；不自动合并或推送任何PR。
- **persisted**：本次规则已整合进 `AGENTS.md`、`03-执行层/README.md`、`task_plan.md`、`计划表.md`、`决策记录.md` 和本日志；结果提交 `9bd475b`。
- **verified**：状态一致性检查 PASS；协作协议和工作项边界的 `git diff --check` PASS；本次提交只包含 `AGENTS.md` 与 `03-执行层/README.md`，未触碰代码、资源或用户已有的三个地图系统文档修改。
- **结论**：PR #2 的核心理念已吸收；PR #2 本身未合并，PR #3 仍只作为待核验外部规范收据。

## 2026-08-18 API 协作审查理念融合

- **accepted**：Human 接受直接参考 PR #3 的理念，融合进当前协作工作流。
- **采用范围**：外部规范作为输入和交付收据；项目权威仍按 `API契约表 → 03-执行层系统卡 → 代码/测试状态 → task_plan` 对照；接口契约状态与工程实现状态分开；`Core Ready` 不等于 `Integrated`；未确定方向保持 `UNKNOWN`/`Draft`。
- **不采用**：不把 PR #3 的收据当成 API 事实；不在缺少源文档时宣称已验证；不直接修改 API 契约、系统卡或代码。
- **persisted**：审查顺序、状态分层和 Human API Gate 已写入 `AGENTS.md`、`03-执行层/README.md` 及状态台账；结果提交 `6da5755`。
- **verified**：状态一致性检查 PASS；`git diff --check` PASS；未修改 API 契约表、系统卡、代码、资源或外部文档。
- **结论**：PR #3 的方法已融入当前工作流，但 PR #3 收据本身仍不是 API 事实；实际外部文档取得后必须另行审查。

## 2026-08-19 浏览器编译预览视觉验收

- **accepted**：Human 明确表示当前本地编译预览验收通过。
- **验证基线**：`npm run build` 通过；资源检查通过；编译产物 preview 在 `http://127.0.0.1:4173/` 启动；browser Smoke 通过，Phaser、地图、玩家、Tilemap 和诊断状态均正常。
- **persisted**：`WI-RENDER-PLAYABLE-001` 关闭，视觉 Gate 状态更新为 `human-visual-verified`；动态状态回到 `current-work-item: none` 的合法选择状态。
- **边界**：本次验收只关闭当前可玩雏形的浏览器/视觉 Gate，不代表完整原站功能或16个正式系统已完成。
- **下一阶段建议**：优先处理审计发现的运行时诊断暴露和 `src/world/world.ts` 写入回滚风险；同时保留验证器、粒子和 favicon 为后续候选。

## 2026-08-19 动态分块运行时授权

- **accepted**：Human 授权开始分步实现动态分块运行时。
- **工作项**：`WI-SYS-CHUNK-WORLD-INTEGRATION-001`。
- **计划**：资源入口 → Chunk 数据协调器（目标集合、请求、缓存、在途去重、过期结果）→ World 事务（apply/remove/回滚/销毁）→ Phaser 动态装卸 → typecheck、测试、build、资源检查和 browser Smoke。
- **范围**：允许 `src/chunk/`、`src/world/`、相关 `game/`、`tests/`、`scripts/`、`package.json` 和派生 `public/`；不改 `sample/`、旧 Phaser 项目，不纳入粒子/NPC/交互/完整特殊13层语义。
- **persisted**：动态分块实现已提交到隔离分支，阶段提交为 `b93da23`、`559d391`、`fc164b4`。
- **verified**：typecheck PASS；142 项测试 PASS；runtime asset check PASS；build PASS；browser Smoke PASS；`browser:chunk-smoke` PASS（移动后新增 `chunk20`–`chunk24` 请求，无异常或失败请求）。
- **Human反馈**：动态预览中街道直接缺失，并有大量黑块，首版视觉 Gate 未通过。
- **诊断**：25 个 chunk 拼接与 `final_map.json` 逐格一致；初始/移动后的 chunk 请求正确；`master.json` 的 exterior `firstgid=1`，但 `game/CampusScene.ts` 注册 exterior 时省略 firstgid，Phaser 空 Tilemap 默认按 0 注册，导致直接写入的 Tiled GID 整体错位。
- **修复**：新增 `tilesetFirstGid`，由 master 读取 `exterior=1` 与 `collisions-objects=69345`，Tilemap 注册不再依赖错误默认值；增加 firstgid 回归测试。
- **verified**：typecheck PASS；143 项测试 PASS；build PASS；runtime asset check PASS；browser Smoke PASS；`browser:chunk-smoke` PASS；修复截图 `C:/tmp/campus-chunk-gid-fix.png`、移动截图 `C:/tmp/campus-chunk-gid-move.png` 显示街道/铁轨/道路/水池恢复。
- **状态**：重新进入 `visual/awaiting-human`；修复尚未提交，等待Human复验，不合并、不推送。

## 2026-08-20 动态分块运行时浏览器复验与空 GID 修复

- **observed**：工作区 Chromium 以无 GPU headless 参数在 CDP `9223` 启动；已有 `4176` 预览的普通 Smoke 捕获到重复 `chunk world failure ... Cannot read properties of undefined (reading '2')`，截图显示诊断面板和黑色地图；资源请求均成功。
- **diagnosis**：Phaser `PutTileAt` 内部按 `tilemap.tiles[gid][2]` 查找 tile；Tiled 空 GID `0` 被直接传入 `putTilesAt`，不是 Phaser 的空 tile `-1`，因此每个 chunk apply 都失败。已有 `4176` 服务未反映当前修复，使用当前构建的干净 `4199` 预览复验。
- **fixed**：`game/PhaserWorldRenderer.ts` 写入 Phaser 前将 GID `0` 映射为 `-1`；不改 `sample/`、原始地图证据或游戏逻辑范围外的系统。
- **verified**：typecheck PASS；145 项测试 PASS；runtime asset check PASS；状态一致性 PASS；build PASS；干净 `4199` 普通 browser Smoke PASS（诊断为空、无 JS exception、资源除外无失败）；`browser:chunk-smoke` PASS（初始 `chunk0`–`chunk19`，移动新增 `chunk20`–`chunk24`）。截图在 `.pi/verification/browser-smoke-clean.png` 和 `.pi/verification/browser-chunk-smoke-clean.png`。
- **状态**：仍为 `visual/awaiting-human`；自动证据已通过，待 Human 视觉确认后提交。`4175` 是正式入口，当前干净证据使用临时 `4199`。

## 2026-08-20 验证器页面资源清理

- **observed**：无 GPU Chromium 使用软件 WebGL 持续运行 Phaser 帧循环；普通 Smoke 每次新建页面但未关闭，连续验证后留下多个地图页面，导致 CPU 和温度升高。
- **fixed**：`scripts/browser-smoke.mjs` 正常结束时关闭自身 CDP target；增加显式的 `SMOKE_CLEANUP_STALE=true` 选项，用于清理同一 CDP 浏览器中残留的 page targets。
- **verified**：使用该选项清理残留页面后，普通 Smoke 在 `4199` 重新 PASS；无异常、无失败请求，截图重新生成。预览服务保留，但不再继续启动浏览器验收。
- **边界**：本次只修验证器资源生命周期，不改变游戏运行逻辑；动态分块视觉 Gate 仍等待 Human 确认。

## 2026-08-20 动态分块视觉 Gate 关闭

- **accepted**：Human 明确表示“视觉验收通过”。接受范围为本次 master+chunk 动态分块有界运行时、GID 映射修复、空 GID 兼容修复和验证器页面清理；不包含墙体/桥真实碰撞、完整 13 层语义或其他后续功能。
- **persisted**：根决策记录、SYS-CHUNK/SYS-WORLD 执行卡、执行层总账、`task_plan.md`、`计划表.md` 已同步为 Human visual verified / 当前无 active 工作项；结果提交为 `b707553`。
- **verified**：typecheck、145 项测试、runtime asset check、build、状态一致性、普通 browser Smoke 和跨块 Smoke 均通过；干净截图已由 Human 视觉确认。
- **状态**：本工作项完成，结果提交为 `b707553`；不 push、不自动启动下一工作项。

## 2026-08-20 墙体与桥真实碰撞集成授权

- **accepted**：Human 明确表示“ok。开始吧”。接受范围为玩家 Arcade Body（20×8、offset 14,36、drag 300、maxVelocity 200）、walls 与四个 bridge wall 层的碰撞接线、`body.blocked` 撞墙反馈和 chunk 装卸后的碰撞清理。
- **persisted**：新增 `DEC-SYS-MOVE-WORLD-COLLISION-001` 与 `WI-SYS-MOVE-WORLD-COLLISION-001`；状态进入 `investigation / implementation-active`，节点为 SYS-MOVE、SYS-LAYER、SYS-WORLD、SYS-CHUNK。
- **boundary**：不纳入手机摇杆、NPC、完整13层、粒子、交互或其他未授权玩法；桥上下层状态来源未证实前保持 UNKNOWN，不猜造触发器。
- **next**：先完成文档激活提交，再实现墙层碰撞；若桥状态来源仍无法证明，分开记录并停在可证实范围。

## 2026-08-20 墙体与桥真实碰撞集成关闭

- **implemented**：提交 `e3f412a` 接入玩家 Arcade Body（20×8、offset 14,36、drag 300、maxVelocity 200）、walls 与四个 bridge wall 层的碰撞注册、`body.blocked` 撞墙反馈和 chunk 清理；walls 按公开 Bundle 事实强制 69345 碰撞、69346 放行。
- **implemented**：桥状态使用公开 Bundle 已定位的 tile 矩形入口/出口和每 3 帧边沿检查；bridge1 重叠区采用入口优先，bridge2 两个区域需离开后再次进入才翻转；上下层碰撞、alpha/可见性和玩家 depth 1650 同步。
- **fixed**：发现并修复 Phaser collider 队列尚未完成时销毁 Tilemap layer 导致 `collideSpriteVsTilemapLayer` 读取已死 tilemap 的错误；现在等待 collider 从 Arcade ProcessQueue 移除，并先从 Tilemap layer 记录移除再销毁对象。
- **verified**：`npm run typecheck` PASS；`npm test` PASS（28 files / 147 tests）；`npm run build`、`check:runtime`、`git diff --check` PASS。
- **verified**：当前 build 在显式 `4200` 预览上通过 `browser:collision-smoke`（真实 blocked.down、100 collision layers、bridge1/2 up/down 与 depth 恢复、无 runtime errors）、`browser:chunk-smoke`（四段可通行键路新增 chunk20–24、无异常）和普通 `browser:smoke`。
- **boundary**：4175 被旧服务占用且不是本次 build，未把它的历史空 GID 错误混入结论；完整24层语义、请求取消、性能量化、NPC/交互和完整 Phaser 生命周期仍未关闭。
- **status**：本工作项完成并回写 `task_plan.md`、SYS-MOVE/SYS-LAYER/SYS-WORLD/SYS-CHUNK 和总账；当前回到 `none / work-item-selection`，不自动启动下一项。

## 2026-08-20 运行时安全有界修复授权

- **accepted**：Human 选择“运行时安全审计（推荐）”，接受 `WI-RUNTIME-SAFETY-001`。
- **persisted**：新增 `DEC-RUNTIME-SAFETY-001`；激活节点为 SYS-WORLD、SYS-CHUNK、SYS-APP，当前进入 `implementation`。
- **scope**：World 同步/异步部分写入回滚；生产 build 关闭可见诊断面板和调试 hooks、开发诊断限量；初始化/renderer teardown rejected 收敛；inline favicon 清理默认入口 404。
- **boundary**：不扩展完整 Phaser resource teardown、完整 13/24 层、粒子、NPC、交互、验证器或远端 Git；碰撞 Smoke 只使用显式 `test-hooks` build。
- **next**：先修改最小实现和回归测试，再分别验证普通 production build 与 test-hooks build；验证完成前不关闭工作项。

## 2026-08-20 运行时安全有界修复关闭

- **implemented**：`src/world/world.ts` 在同步/异步 write/clear hook 调用前登记当前层；hook 即使部分改变外部状态后抛错，补偿回滚也会尝试处理当前层；新增同步/异步回归测试。
- **implemented**：`index.html` 移除生产内联诊断监听，`game/runtimeDiagnostics.ts` 仅开发环境安装且最多保留50行；`CampusScene` 只在 development 或显式 `test-hooks` build 暴露诊断/碰撞 hooks，并在 shutdown 比较引用后清理。
- **implemented**：初始化和 renderer 销毁 rejected 显式 catch；入口加入 inline favicon，消除 `/favicon.ico` 请求；Smoke 工具支持显式 URL 参数，保留默认4175兼容。
- **verified**：`npm run typecheck` PASS；`npm test` PASS（28 files / 149 tests）；`npm run build` 与 `npm run build:test-hooks` PASS；`git diff --check` 和状态一致性 PASS。
- **verified**：普通 production build 在 `4201` 通过 `browser:runtime-safety-smoke`（诊断为空/hidden、两个 debug hooks undefined、无异常/失败请求/错误响应）、普通 `browser:smoke` 和 `browser:chunk-smoke`；显式 `test-hooks` build 在 `4202` 通过 `browser:collision-smoke --bridge-test`（真实 blocked、bridge1/2 状态/depth、无 runtime errors）。
- **boundary**：本次只关闭运行时安全有界范围；完整24层语义、请求取消、多视口性能、完整 Phaser resource teardown、粒子、NPC、交互和验证器仍未关闭。
- **status**：结果提交 `632a0c9`；系统卡、SYS-APP bounded note、总账、决策记录和 `task_plan.md` 已同步；当前回到 `none / work-item-selection`，不自动启动下一项。

## 2026-08-20 内容线 SYS-ZONE 逆向与设计激活

- **accepted**：Human 对“内容先做”回复 `ok。开始吧`；将下一项收敛为 `WI-SYS-ZONE-DESIGN-001`。
- **scope**：只核对公开 marker/区域触发来源、坐标/区域边界、进入/离开和防重复行为；补 `04-内容层/作品集内容.md` 内容索引、SYS-ZONE 七格、接口和未知队列。
- **boundary**：不写 `src/`/`game/` 正式代码；不推进 SYS-INTERACT 弹窗；不猜未证实文案、资源、坐标或触发器；不修改 `sample/`、旧 Phaser 或远端 Git。
- **status**：已新增 `DEC-SYS-ZONE-INVESTIGATION-001`；`task_plan.md` 已进入 `evidence-and-design` / `investigation`；下一步查证公开证据并形成设计候选，之后交 Human review。

## 2026-08-20 SYS-ZONE 公开证据调查结果（草案）

- **FACT**：11 个 marker（sunburn×5、vortex×6）直接硬编码在 `sample/original-public-build/mirror/chunk-WMFY56ZM.js` 的 `markers` 配置，不来自地图 JSON object layer；精确 id、名称、`menuId` 和像素坐标已写入 `04-内容层/作品集内容.md`。
- **FACT**：marker 中心与玩家距离严格 `<30px` 触发；场景每 100ms 检查当前视口外扩 100px 内 active marker；离开后隐藏并重置手动关闭状态；`visited` 不阻止再次访问；驻留内手动关闭不会重开。
- **FACT**：marker 通过 `menuId` 调用 `window.showModalWithBackdrop`；`pointerdown` 的 `handleClick()` 为空；未发现 marker 使用 `JustDown` 的证据。
- **FACT**：英语首页有 11 个 marker 对应 modal DOM；`big-map` 与 `under-hood` 是独立 modal；首页有 `/sk/` alternate，Bundle 有 `en`/`sk` 翻译表，但 `/sk/` 完整页面和逐项内容仍 UNKNOWN。
- **INFERRED**：SYS-ZONE 设计候选为静态 marker 配置、100ms 距离检查、进入/离开状态、visited/手动关闭状态；弹窗 DOM、暂停恢复和文案归 SYS-INTERACT / SYS-GAME-UI。
- **persisted**：已修正 `04-内容层/作品集内容.md` 的错误数量口径（不是“10 个模态”，而是 11 个 marker 内容窗口 + 2 个独立面板）；已填写 SYS-ZONE 调查草案、API Draft，并关闭 `Q-ZONE-001`，保留 `/sk/`、动态 marker、失败/销毁策略残余 UNKNOWN。
- **next**：核对链接、结构和状态一致性，形成 Human review 包；本工作项仍 active，尚未晋升 SYS-ZONE 为 designed，也未授权代码。

## 2026-08-20 SYS-ZONE 设计评审通过

- **accepted**：Human 回复 `通过`，接受 SYS-ZONE 设计候选。
- **decision**：静态 Bundle marker 配置、严格 `<30px`、100ms active-marker 轮询、进入/离开、visited/手动关闭分离、`menuId` → SYS-INTERACT 语义冻结；marker 不从地图 object layer 推导。
- **boundary**：`/sk/` 完整内容、动态 marker、缺失 modal、异步失败和销毁策略继续保持 UNKNOWN；正式代码、SYS-INTERACT 弹窗和完整双语内容不在本次授权。
- **persisted**：SYS-ZONE 卡准备晋升 `designed`，理解层进度更新为 9/16 详细设计，API 区域接口从 Draft 移入已冻结契约；待关闭状态和结果提交同步。

## 2026-08-20 SYS-ZONE 设计工作项关闭

- **implemented**：本项只落盘设计文档，不写正式运行时代码；SYS-ZONE 卡状态为 `designed`，内容入口索引、理解层进度、API 语义和未知队列均已同步。
- **verified**：`python3 scripts/check-state-consistency.py` PASS；`git diff --check` PASS；独立只读复核 PASS；公开 Bundle/HTML 证据路径和 11 个 marker 索引可追溯。
- **boundary**：正式 SYS-ZONE 实现、SYS-INTERACT 弹窗、完整双语内容、动态 marker、失败/销毁策略仍未授权或 UNKNOWN。
- **status**：设计结果提交 `05c2274`；`WI-SYS-ZONE-DESIGN-001` 已关闭；`task_plan.md` 回到 `none / work-item-selection`，不自动启动下一项。

## 2026-08-20 地图线 SYS-LAYER 运行时语义收束激活

- **accepted**：Human 回复“继续收束地图线吧”，接受沿 P0 地图线继续推进。
- **route**：依文档地图线“资源 → 世界 → 图层 → 分块”顺序，当前先处理 SYS-LAYER 的完整运行时语义，不继续 P1 内容线。
- **scope**：核对 24 层 apply/remove 对称边界、roof、footsteps、particles3 和 SYS-WORLD/SYS-CHUNK 接口；优先处理 `Q-LAYER-002`，特殊 13 层卸载仍归 SYS-WORLD UNKNOWN。
- **boundary**：本项先做公开证据、未知项和验收范围收敛，不写正式 `src/`/`game/`，不扩展请求取消、性能、完整 teardown、完整 World/Chunk 生命周期或玩法线。
- **status**：已建立 `DEC-SYS-LAYER-RUNTIME-SEMANTICS-001` 和 `WI-SYS-LAYER-RUNTIME-SEMANTICS-001`；`task_plan.md` 进入 `evidence-and-design` / `investigation`。

## 2026-08-20 SYS-LAYER 首轮运行时语义调查结果

- **FACT**：Bundle 独立加载 `particle-trajectories.json`，`createAreaParticleEmitters`、`initCrowdStaticNPCs` 和 `updateProtestersRisingNPCs` 消费 trajectory region；未找到读取 `particles3`/GID 69361 或 marker → trajectory 的直接步骤。
- **INFERRED**：particles3 的 86 个 marker 与 `protesters_rising` 的计数/空间关系很可能相关，但不能当作直接转换事实。
- **UNKNOWN**：`Q-LAYER-002` 保持 open；特殊 13 层卸载边界仍归 SYS-WORLD 生命周期 UNKNOWN。
- **当前代码差距**：`src/layer/` 只有 24 层名称/角色/depth 的确定性策略；`game/PhaserWorldRenderer.ts` 当前跳过全部 marker 层，尚未建立 marker 撤销、roof 状态、footsteps 输出或 particles3 诊断接口。
- **候选范围（待 Human review）**：收敛视觉层/roof/marker/footsteps 的 apply/remove 对称结果和失败边界；particles3 保留并报告未消费；不实现动态抗议人群、cars/NPC、请求取消、性能或完整 teardown。
- **verified**：独立只读复核 PASS；状态一致性 PASS；`git diff --check` PASS；当前没有 `src/`、`game/` 或 `sample/` 变更。
- **gate**：候选设计包已就绪，`task_plan.md` 已推进至 `human-design-review`；等待 Human 评审，不自动进入实现。

## 2026-08-20 SYS-LAYER 有界实现授权

- **accepted**：Human 回复“继续”，接受本轮 SYS-LAYER 收束设计并授权进入明确范围的实现。
- **new work item**：关闭设计阶段 `WI-SYS-LAYER-RUNTIME-SEMANTICS-001`，激活 `WI-SYS-LAYER-RUNTIME-SEMANTICS-IMPLEMENT-001`。
- **allowed**：修改 `src/layer/`、必要的 `src/world/` contract/hooks、`game/PhaserWorldRenderer.ts`、必要 `game/CampusScene.ts`、`scripts/sanitize-runtime-maps.mjs`、对应测试和验证脚本，实现 visual/roof/marker/footsteps 的 apply/remove、失败边界和 particles3 未消费诊断；sanitizer 只保留 marker 层数据，不引入粒子贴图。
- **forbidden**：不修改 `sample/` 或旧 Phaser；不实现动态抗议人群、cars/NPC、特殊13层生命周期、请求取消、性能、完整 teardown 或其他玩法线。
- **status**：当前进入 `implementation / implementation`；实现代码已写入工作树，尚未提交，正在做真实资源和浏览器回归。

## 2026-08-20 SYS-LAYER 实现中发现的资源边界

- **FACT**：原始 `chunk8/9.json` 的 `particles/particles2` 层含 GID 69360；公开 Bundle 只定位到 69355/69359，未定位 69360 的直接处理。
- **DECISION**：运行时 sanitizer 保留已确认 marker GID（含 particles3=69361），对无公开消费者的外部 69360 裁剪并输出计数；不引入粒子 tileset，不把 69360 当成已理解行为。
- **verified**：`npm run typecheck` PASS；`npm test -- --run` PASS（30 files / 156 tests）；`npm run build` 和 `npm run build:test-hooks` 的 runtime asset check PASS；production browser Smoke/chunk Smoke/runtime-safety Smoke PASS；test-hooks `browser:layer-smoke` PASS（2582 marker records、86 particles3 diagnostics、0 failed chunks）。
- **residual**：`Q-LAYER-002` 和新增 `Q-LAYER-003` 保持 open；完整 particles、NPC/车辆消费者和完整地图生命周期不在本项。

## 2026-08-20 SYS-LAYER 有界实现验证与关闭

- **implemented**：`10c7d88` 落盘 `visual/roof/marker/footsteps` 的 chunk-owned apply/remove；24 层策略声明 marker GID 白名单；未知 marker GID 生成诊断并使 World apply 回滚；roof 保存 concert/factory 分组状态与 300ms 参数；footsteps 以 chunk tilelayer marker 为运行来源；particles3 保留并报告未消费。
- **implemented**：sanitizer 保留已确认 marker GID，裁剪无公开消费者的外部 GID（包括 69360）并输出计数；没有引入粒子 tileset、动态抗议人群、NPC 或车辆消费者。
- **verified**：`npm run typecheck` PASS；`npm test -- --run` PASS（30 files / 157 tests）；`npm run build`、`npm run build:test-hooks`、资源检查和 `python3 scripts/check-state-consistency.py` PASS。
- **verified**：production `4203` 的 `browser:smoke`、`browser:runtime-safety-smoke` PASS；test-hooks `4204` 的 `browser:layer-smoke`（2582 marker records、86 particles3 diagnostics、0 failed chunks）、`browser:collision-smoke --bridge-test` 和 `browser:chunk-smoke` PASS。production 下 layer Smoke 因诊断 hook 按安全边界不存在，不作为失败证据。
- **independent review**：`lightweight-verifier` 确认 typecheck、157 项测试、资源检查、build、apply/remove 对称、未知 marker 回滚、particles3 未消费、roof 300ms 幂等和范围边界均 PASS；其首次报告的浏览器环境缺失已由上述显式端口 Smoke 补齐。
- **boundary**：完整 SYS-LAYER 节点仍保持 `designed`；`Q-LAYER-002`/`Q-LAYER-003`、特殊 13 层生命周期、请求取消、性能量化、真实 roof 区域/tween 消费和完整 Phaser teardown 未关闭。
- **status**：`WI-SYS-LAYER-RUNTIME-SEMANTICS-IMPLEMENT-001` 已关闭，当前 `task_plan.md` 回到 `none / work-item-selection`；不自动启动下一项。

## 2026-08-20 SYS-WORLD/SYS-CHUNK 地图生命周期收口激活

- **accepted**：Human 回复“ok。开始吧，按照你说的来”，接受先收口地图生命周期，再推进玩法线的顺序。
- **decision**：建立 `DEC-SYS-MAP-LIFECYCLE-CLOSURE-001`，当前工作项为 `WI-SYS-MAP-LIFECYCLE-CLOSURE-001`。
- **scope**：为 master/chunk 请求接入可取消 signal；销毁时阻止新请求和过期结果回写；等待排队/活动 mutation；显式清理 collider、Tilemap layer、Tilemap 和运行时状态；用固定浏览器场景记录请求去重与渲染层/marker 上界。
- **boundary**：不修改 `sample/` 或旧 Phaser；不把原站特殊13层、完整24层消费者、FPS/内存性能、NPC/车辆/粒子或玩法实现写进本项。
- **initial finding**：现有 ChunkDataStore/Coordinator 已有同坐标 in-flight 去重、目标过期守卫和基础 destroy 守卫；缺口是 AbortSignal、异步 destroy 收敛、scheduler active mutation 等待、renderer map destroy 和固定生命周期验收。
- **status**：已持久化到 SYS-WORLD、SYS-CHUNK、根决策记录和 `task_plan.md`，当前进入 `implementation / implementation`；尚未修改正式代码，下一步实现并补回归测试。

## 2026-08-20 SYS-WORLD/SYS-CHUNK 地图生命周期收口完成

- **implemented**：`d61faa1` 为 `JsonLoader` 接入可选 `AbortSignal`；DataStore 销毁时取消请求并清空 cache/master/failure；Coordinator 增加 `destroyAsync()`、过期 mutation 补偿和销毁后状态清理；scheduler 支持排队清空与 active idle 等待；renderer 显式清理 collider/layer/Tilemap；CampusScene 固定 shutdown 顺序并保护已释放 player body/physics world。
- **implemented**：新增生命周期浏览器 Smoke；修正 collision Smoke 的 flag-only 参数解析并自动补 collision-test 查询参数。
- **verified**：`npm run typecheck` PASS；`npm test -- --run` PASS（31 files / 164 tests）；`npm run build`、`npm run build:test-hooks`、资源检查和 `python3 scripts/check-state-consistency.py` PASS。
- **verified**：test-hooks `browser:lifecycle-smoke`、`browser:layer-smoke`、`browser:collision-smoke --bridge-test`、`browser:chunk-smoke` PASS；production `browser:smoke`、`browser:runtime-safety-smoke` PASS。生命周期 Smoke 记录 20 个 chunk、380 个渲染层、2582 个 marker，均低于 25/475/4109 上界；销毁后 debug/collision/lifecycle hooks 移除，异常和失败请求为 0。
- **independent review**：独立验收确认取消不回写、destroyAsync、scheduler/renderer teardown、scene shutdown、范围边界均 PASS；其 collision flag-only 参数意见已修正并复跑通过。`src/chunk/index.ts` 属于本项明确允许的 `src/chunk/` 范围，不是越界修改。
- **boundary**：完整24层消费者、原站特殊13层事实、多视口严格 FPS/内存量化、NPC/车辆/粒子和玩法线仍未解决；固定场景数量上界不等于完整性能结论。
- **status**：`WI-SYS-MAP-LIFECYCLE-CLOSURE-001` 已关闭，结果提交 `d61faa1`；`task_plan.md` 回到 `none / work-item-selection`，不自动启动下一项。

## 2026-08-20 SYS-INPUT 真实运行时接入完成

- **accepted**：Human 回复“ok。开始吧”，接受 `DEC-SYS-INPUT-TOUCH-001`，激活 `WI-SYS-INPUT-TOUCH-001`。
- **decision**：仓库没有 rexVirtualJoystick 运行资源或依赖；本次采用原生 Phaser pointer 适配器，只复现已确认的固定参数、单指、8方向、死区和优先级行为，不冒充原站插件实现。
- **implemented**：`66a20f8` 新增 `game/PhaserVirtualJoystick.ts`；移动/平板显示固定右下摇杆，桌面隐藏；单指 ownership、第二指忽略、resize 重定位、forceMin 死区、shutdown listener/Graphics 清理；`CampusScene` 接入 `resolveMovement`，摇杆 active 时键盘让位，释放后恢复。
- **implemented**：新增 `tests/input/phaser-virtual-joystick.test.ts` 和 `scripts/browser-mobile-input-smoke.mjs`；test-hooks debug 仅报告输入状态，production 不暴露 hooks。
- **verified**：`npm run typecheck` PASS；`npm test -- --run` PASS（32 files / 168 tests）；`npm run build`、`npm run build:test-hooks`、资源检查和 `python3 scripts/check-state-consistency.py` PASS。
- **verified**：移动输入 Smoke PASS：desktop hidden、mobile visible、首指移动输出 east、第二指不接管、摇杆按下屏蔽 ArrowLeft、释放后 ArrowRight 恢复 150 速度；无异常/失败请求。普通/安全/地图/碰撞/生命周期 Smoke 亦通过。
- **boundary**：完整 SYS-INPUT 节点仍保持 `designed`；原站阈值单位和键盘长按时序保持 UNKNOWN；不实现玩家状态、SYS-ZONE/SYS-INTERACT、NPC/车辆/粒子或通用输入框架。
- **status**：`WI-SYS-INPUT-TOUCH-001` 已关闭，结果提交 `66a20f8`；`task_plan.md` 回到 `none / work-item-selection`，不自动启动下一项。

## 2026-08-20 并行调查 worktree 方案激活

- **accepted**：Human 回复“ok。开始创建worktree吧”，接受 `DEC-PARALLEL-WORKTREE-001`。
- **scope**：第一批建立 A SYS-LAYER 消费者、B SYS-WORLD 特殊13层、C SYS-ASSET 加载、D 多视口/性能基线四个隔离调查 worktree；只做证据调查和基线，不开始正式代码。
- **isolation**：从 `1d0db48` / `master` 建立独立本地分支；主工作树保留总控与权威文档，调查窗口不共改主目录，不修改 `sample/`、`task_plan.md`、`决策记录.md`、总账或系统卡，不执行 reset/clean/pull/push。
- **handoff**：每个窗口返回 FACT/INFERRED/UNKNOWN、证据路径、冲突、建议 DECISION 和验证命令；Human 将报告粘贴回本窗口，由 Main 统一核对、落盘和进入下一 Human Gate。
- **implemented**：从 `1d0db48` 创建四个隔离分支和 worktree：`.pi/worktrees/layer-consumers` (`recon/layer-consumers`)、`.pi/worktrees/special-layers` (`recon/special-layers`)、`.pi/worktrees/asset-loading` (`recon/asset-loading`)、`.pi/worktrees/perf-baseline` (`recon/perf-baseline`)；`git worktree list` 已确认四个独立条目。
- **verified**：四个 worktree 均从 `1d0db48` 建立；主工作树保持 `master`，只承载总控和权威状态。嵌套 worktree 的单独 status 命令被 WSL 沙箱拦截，未尝试绕过；worktree list 是当前可复核证据。
- **status**：`task_plan.md` 已从 `worktree-setup` 推进到 `parallel-recon`；等待四个窗口启动并返回报告。

## 2026-08-20 玩法线串行窗口决定

- **accepted**：Human 回复“那个玩法线就一个窗口吧，一步步串行实现”，接受 `DEC-GAMEPLAY-SERIAL-001`。
- **scope**：玩法线只保留一个专用窗口和一个独立 worktree，顺序为 `SYS-PLAYER → SYS-CAMERA`；每阶段完成实现、测试、浏览器验证和 Human Gate 后才推进下一阶段。
- **boundary**：不与 A-D 地图调查窗口共改文件；SYS-ZONE/SYS-INTERACT 等内容线不自动并入；玩法 worktree 当前只规划，不创建、不启动代码。
- **status**：当前仍执行 `WI-PARALLEL-MAP-RECON-001`；Human 已授权创建唯一玩法 worktree，后续仍需另行激活 `WI-SYS-PLAYER-RUNTIME-001` 才能开始 SYS-PLAYER 代码。

## 2026-08-20 创建玩法串行 worktree

- **accepted**：Human 回复“ok。那就开始创建那个玩法线的worktree吧那就。到时候一起合并”，接受 `DEC-GAMEPLAY-WORKTREE-001`。
- **scope**：创建一个 `impl/gameplay-serial` worktree；SYS-PLAYER 与 SYS-CAMERA 在同一窗口串行完成，阶段间经过测试、浏览器验证和 Human Gate。
- **boundary**：创建 worktree 不等于正式实现授权；不与 A-D 地图调查分支共改，不自动合并，不并入 SYS-ZONE/SYS-INTERACT。
- **status**：先提交本次状态，再创建实际 worktree；创建后记录路径和 `git worktree list` 证据。