# 原网站公开发布文件

此目录只保存原网站公开发送给浏览器的发布文件，例如 HTML、CSS、打包后的 JavaScript、地图 JSON、图片、tileset、字体和音频。

- `mirror/`：按原网站公开 URL 路径保存的唯一文件镜像，不按资源类型重新移动。
- `manifest.json`：已保存文件的总清单。
- `network/`：浏览器网络请求清单与未获取资源记录。

这里不代表、也不应被视为原网站的完整原始项目源码。
