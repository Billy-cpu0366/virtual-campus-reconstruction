# Src

此目录预留给正式重构源码，也是以后记录当前有效工程入口的位置。

`SYS-CHUNK` 详细设计已经通过 Human 审查。当前仅建立了 `WI-SYS-CHUNK-CORE-001` 的 proposed 实施授权包，正式实现仍未授权，Node、包管理器、TypeScript 和测试工具也尚未成为 accepted 工程决定。

只有授权包 `approved`、Human 授权决定 `accepted`、`task_plan.md` 中对应工作项为 `active`、授权范围一致且激活状态已进入 clean Git 基线后，才能建立正式源码。Phaser、Vite和浏览器集成当前均未授权。
