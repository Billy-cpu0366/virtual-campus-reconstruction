---
tags:
  - 虚拟校园
  - 暗号层
  - 接口
type: handoff
created: 2026-08-14
---

# 接头暗号表（系统之间的握手约定）

> **两个系统"握手"的暗号，全部写在这。** 框先定死，细节后补；对好暗号，两个人背对背干活也不乱。
> 铁规矩：**歧义不能留在接口里**——这里每一条都要精确。

---

## 一、已定死的接头

| 谁 → 谁 | 暗号（约定） | 状态 |
|---|---|---|
| 地图 → 所有人 | 地图 = 5×5 块，每块 28×28 格；块编号 `index = y*5 + x`；文件名 `chunk-{index}.json` | ✅ 已定死 |
| 移动 → 地图 | "这格能走吗"：输入 (x, y) → 输出 true / false | 📐 说明书先给，地图盖好再通电 |
| 分块 → 世界 | 放块 / 撤块：`applyChunk` / `removeChunk` | 📐 图纸已画 |
| 世界 → 所有人 | 建世界 / 拆世界：`createWorld` / `destroyWorld` | 📐 图纸已画 |

## 二、还没定死（待对暗号）

| 谁 → 谁 | 待定什么 | 状态 |
|---|---|---|
| 资源 → 各系统 | 图片怎么加载、失败怎么办、要不要缓存 | ❌ 待定 |
| 相机 → 分块 | 相机范围怎么给分块（决定加载哪几块） | 【TBD】 |

## 三、精确值速查（来自旧仓库，冲突时以旧仓库为准）

| 项 | 值 | 备注 |
|---|---|---|
| 世界尺寸 | 140×140 格 × 16px = 2240×2240 px | 【TBD：学习导图与 PRD 一致】 |
| 图层 | 24 层（layer1-10 + walls + cars + roof + bridge + particles + footsteps） | 【TBD：13 图层语义待核】 |
| chunk 尺寸 | 28×28 格（旧仓库）/ 32（学习导图实测 chunkWidth） | 【TBD：两处冲突，待强 AI 核】 |
| chunk 可见范围 | 玩家 3×3 邻域 ∪ 相机范围 +1 | 来自旧仓库 task_plan |

---

## 冻结规矩

说"行"之后不能默默改；要改，就**一起改这张表**，改完两边同步代码和文档，别留旧规矩坑人。
