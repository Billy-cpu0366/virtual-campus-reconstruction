---
title: 总文档迁移：本轮怎样合并文档入口（治理任务卡）
type: work-item-task-card
status: proposed
version: v0.1
work-item-id: WI-DOC-PORTAL-MIGRATION-001
work-item-level: level-1
work-item-type: governance
work-item-status: proposed
node-refs: not-applicable
authorization-ref: DEC-DOC-PORTAL-MIGRATION-001
current-gate: none
gate-status: not-applicable
main-definition: false
strict-pa: true
updated: 2026-08-14
---

# 总文档迁移：本轮怎样合并文档入口（治理任务卡）

> **一句话：把独立的人话文档层复制进当前 Git 仓库，在不重组精确工程文档、证据和代码的前提下，建立一个可追溯的总入口。**

## 1. 来源与目标

执行来源：`D:\复盘\虚拟校园项目·总文档\00-迁移结合路线.md`，SHA-256：`20bde27e97b7ec2bda2753bf1f31d33fa0baf186da43756661001de1d97825cd`。

目标仓库：`D:\复盘\虚拟校园项目重构`。安全起点为提交`a2e303b`；原计划标记的`03fe5ac`已由一笔独立、仅格式化的基线提交前移。

目标结构：

- 仓库根增加`01-理解层/`至`05-素材/`和三份人话入口；
- 原有`doc/v0.1/`继续保存精确工程知识；
- `sample/`继续保存原站证据；
- `src/`与`tests/`继续保存代码和测试；
- 根`README.md`成为Human与AI共同入口；
- 动态状态仍只认根`task_plan.md`，Human入口只翻译和链接。

## 2. 允许范围

1. 从总文档目录复制以下内容到仓库根：
   - `01-理解层/`、`02-暗号层/`、`03-执行层/`、`04-内容层/`、`05-素材/`；
   - `计划表.md`、`术语表.md`、`决策记录.md`。
2. 修正迁入文档的仓库内相对链接和“旧仓库”称呼。
3. 重写根`README.md`，说明五层入口、SSOT、当前Phase 1边界和历史/未来目录。
4. 在`AGENTS.md`启动链中接入人话层入口，但不改变`doc/v0.1/README.md`的机器入口地位。
5. 为本次迁移维护决策、任务状态、过程记录和必要的治理检查合同。
6. 使用独立提交保存：既有格式化基线、准备、激活、原样导入、指针/入口改造和关闭验证。

## 3. 明确禁止

- 不重组或搬动`doc/v0.1/`、`sample/`、`src/`、`tests/`的既有内容；本任务自身的治理记录除外。
- 不修改正式代码、测试逻辑、原站镜像或冻结证据。
- 不搬入来源目录的`README.md`、`00-迁移结合路线.md`或`.obsidian`配置。
- 不搬迁`D:\复盘\虚拟校园项目`；只在新根入口标明它是历史规划和Phase 2材料。
- 不删除、改名或清空`D:\复盘\虚拟校园项目·总文档`。
- 不执行路线第8步清理；清理必须再次取得Human确认。
- 不推送远端。

## 4. 严格迁移步骤

1. 确认工作区clean并记录安全基线；
2. 提交本任务卡、accepted决定和治理合同准备；
3. 暂停视觉补证工作项并原子激活本工作项；
4. 原样复制26份迁入文档，确认工程目录没有变化后单独提交；
5. 修正同仓库指针、SSOT声明、根入口和AGENTS入口；
6. 扫描残余旧路径，逐项区分历史注记和错误物理路径；
7. 运行治理检查、链接检查、`npm test`、`npm run typecheck`和Git差异审计；
8. 记录结果提交并关闭本工作项，恢复视觉补证工作项到Human审查状态。

## 5. 退出标准

- 26份指定文档全部进入仓库，来源目录仍完整保留；
- 根README可以到达五层、人话计划、机器任务、精确文档、证据和代码；
- 迁入文档不存在错误的`../虚拟校园项目重构/`物理路径；
- `doc/v0.1/`、`sample/`、`src/`、`tests/`既有内容未被重组；
- 自动治理检查通过；
- 26项测试和类型检查通过；
- Git差异仅包含批准的文档迁移、入口、治理记录和检查合同；
- 结果进入clean Git提交；
- 原总文档目录未删除，路线第8步仍等待Human单独确认。

## 6. Human停点

本任务已经由Human明确回复“ok。开始把”授权执行第0至第7步。完成验证后可以关闭迁移工作项并恢复此前暂停的视觉补证审查；旧目录清理必须停下并再次询问Human。
