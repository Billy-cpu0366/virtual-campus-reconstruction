---
title: 文档有没有对齐（治理检查报告）
type: governance-verification-report
status: approved
verification-status: verified
version: v1.1
updated: 2026-08-11
verified-at: 2026-08-11
baseline-commit: df93d97f2ff8dfb88d54dd58f2d58351bd7c0720
checker-mode: pilot
---

# 文档有没有对齐（治理检查报告）

> **一句话：这份文件记录文档链接、状态和规则是否一致；它不证明游戏功能已经正确。**

> 本文分别记录自动结构检查与 Human/AI 语义审查。自动 PASS 不能代替事实、边界、复用和验收质量判断。本报告记录 `DEC-DOC-GOV-001`、`DEC-WORK-RELAY-001` 与 `DEC-WORK-RELAY-002` 的结构同步；它不自动授权新的正式代码，也不替代项目最终覆盖验收。

## 1. 验证范围

- 决策：`DEC-DOC-GOV-001`、`DEC-WORK-RELAY-001`、`DEC-WORK-RELAY-002`；
- 模式：`pilot`；
- 受影响文件：以 [[../谁确认过什么（来源与决策记录）]] 中对应决定清单为准；
- 当前工作项：`none`；`WI-SYS-CHUNK-CORE-001` 已完成并进入关闭索引；
- 当前阶段：`work-item-selection`；
- 当前 Gate：`none / not-applicable`；
- 当前允许：维护既有 CORE 结果，并按 P0、节点、依赖、未知和差距整理下一项候选；
- 当前禁止：新的正式代码、旧 Phaser、Phaser/Vite/浏览器集成、网络/缓存/渲染、通用框架和证据刷新。

## 2. 自动结构检查

上一治理 clean 基线为 commit `63831821b35cd60d37a3955db642695809c9f889`。本轮首次工作项同步内容提交为 `e9c0c552673a5d32d7dea860978c4a214ebac12a`；提交后已在 clean 工作区复验。

| 项目 | 命令或依据 | 状态 | 结果 |
|---|---|---|---|
| Python 语法 | 内存 `compile(...)` | passed | `PYTHON_COMPILE_PASS`；不生成未跟踪缓存 |
| 治理同步检查 | `python sample/tools/check-doc-governance.py --mode sync` | passed | clean 内容基线 840 项结构检查 PASS（exit 0） |
| 治理试点结构检查 | `python sample/tools/check-doc-governance.py --mode pilot` | passed-structure | clean 内容基线 845 项结构检查 PASS（exit 0）；不等于真实工作项试点完成 |
| Markdown 与链接 | 同上 | passed | 围栏、表格、Wiki/Markdown 文件和锚点解析通过 |
| 当前工作项合同 | 同上 | passed | 当前 WI 为 `none`；检查器已验证零或一个当前 WI、选择状态、关闭索引和正式代码禁写边界 |
| CORE 关闭事务预检 | 内存 `compile(...)`、`--mode sync`、`--mode pilot`、`git diff --check` | passed-preflight | dirty 工作树中 Python PASS、sync 851项、pilot 856项 PASS；计数包含按变更路径执行的写入边界检查 |
| CORE 关闭 clean 复验 | `--mode sync`、`--mode pilot`、`git diff --check` | passed | 内容提交 `df93d97`：sync 840项、pilot 845项 PASS；工作区 clean |
| 来源哈希 | 同上 | passed | 6份受控外部材料在本机均与台账 SHA-256 匹配 |
| 保护路径 | Git 状态与检查器 | passed | 正式 `src`、`src/README.md`、证据镜像与旧 Phaser 零改动；只修改治理文档、过程记录和治理检查器 |
| 独立复核 | lightweight-verifier 只读复核 | passed-preflight | 无 blocking/high/medium 缺陷；正式代码、证据快照零修改，授权前 activation 按预期阻塞 |
| CORE 原子激活 | `python sample/tools/check-doc-governance.py --mode activation` | passed | 基线 `981a21e54ed7cd3eac3ec3bc5b26ce12bd8086a4`：839 项 PASS；HEAD 含 active 状态且工作区 clean |
| 激活后同步检查 | `--mode sync` / `--mode pilot` | passed | 同一 clean 基线：sync 835 项、pilot 840 项 PASS；真实工作项仍需实现和关闭 |
| CORE 类型与行为 | `npm run typecheck` / `npm test` | passed | 结果提交 `f04568f953821e8cc56c33a694171ddab759051f`：strict typecheck PASS；3文件26项测试 PASS |
| 供应链审计 | `npm audit --registry=https://registry.npmjs.org` | passed | 0 vulnerabilities；本机默认 npmmirror 不提供 audit 端点，已显式使用官方 registry |
| CORE 结果治理 | `--mode sync` / `--mode pilot` | passed | clean 结果基线：sync 835 项、pilot 840 项 PASS；授权路径外和 `sample/` 零修改 |
| CORE 独立复核 | lightweight-verifier 只读复核 | passed | 无 blocking/high/medium/low 缺陷；旧 Phaser 零修改；私有合并 helper 不再对外暴露 |

## 3. 语义审查

| 问题 | 状态 | 当前结论 |
|---|---|---|
| 原站事实与重构决定是否分开 | passed | 节点证据等级不允许 `DECISION`；重构决定通过决策引用追踪 |
| 台账是否把培训示例误写成要求 | passed | 转写登记为 `suggestion`，结构和图片登记为 `example`；只有 Human 会话形成 accepted 决定 |
| 节点范围和候选去向是否符合当前范围 | passed | 已知 `CAP-*` 全部有节点去向，未来校园/后台保持 out-of-scope，不批量创建详细文档 |
| 当前阶段与 Human Gate 是否一致 | passed | CORE 已关闭；当前为无 Gate 的工作项选择状态，未产生新的实现授权 |
| 唯一所有权和历史摘要是否清楚 | passed | task_plan、决策台账、节点总账和正式主定义职责分开；Q-MAP关闭项仅保留审计摘要 |
| 实施成本是否保持最小 | passed | 复用现有五类目录，只增加1份台账、1份报告和1个标准库检查器 |
| 独立只读复核 | passed | lightweight-verifier 在 clean 内容提交 `df93d97` 复核来源、11项受影响文件、关闭索引、候选来源和代码边界；无 blocking/high/medium 缺陷 |

## 4. 未通过与未执行的验收

| 验收 | 状态 | 原因 |
|---|---|---|
| `sync` 当前制度同步 | passed | 内容提交 `df93d97` 在 clean 工作区完成 840 项检查 |
| `pilot` 结构表达检查 | passed | 同一 clean 内容提交完成 845 项检查 |
| 轻量任务接力真实试点 | verified | 授权、激活、实现、验证、合法关闭和无当前 WI 的选择状态均已走通 |
| `final-coverage` 项目最终覆盖 | not-passed（按预期） | 当前范围节点尚未全部实现和验证 |
| SYS-CHUNK CORE实施授权 | implementation-verified | `DEC-SYS-CHUNK-CORE-001` 已接受；结果提交、26项测试、审计和独立复核均通过 |

## 5. 基线与结果

| 字段 | 值 |
|---|---|
| 当前内容提交 | `df93d97f2ff8dfb88d54dd58f2d58351bd7c0720`；tree `735b90c8e651828980bb50b5e55f6ee14d780677` |
| 检查日期 | 2026-08-11 |
| 自动检查 | clean 内容基线：Python compile PASS、`sync` 840项、`pilot` 845项、`git diff --check` PASS |
| 语义审查 | Main 已确认零或一个当前 WI 不改变候选来源、Human Gate、代码授权或 SYS-CHUNK 节点状态；没有新建第二套待办 |
| 独立复核 | lightweight-verifier PASS；无 blocking/high/medium 缺陷；正式 `src`、证据镜像和旧 Phaser 零修改 |
| 当前同步结论 | `verified`；CORE 已合法关闭，当前 `current-work-item: none`，等待 Human 选择下一项 |

## 6. 变更后的失效规则

本报告完成后，如决定内容、AI 必读入口、节点/模板合同、当前 Gate、检查器或受检范围发生语义变化，`verification-status` 必须回到 `pending` 并重新生成基线。

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-08-10 | dirty transaction preflight：596 项检查 PASS，报告保持 pending |
| v0.2 | 2026-08-10 | clean 内容基线 `63831821` 验证完成并记录证据 |
| v0.3 | 2026-08-10 | 阶段6B Gate 变化触发失效规则；保留上一基线为历史，等待复验 |
| v0.4 | 2026-08-10 | 阶段6C通过并进入实现授权审查；更新当前 Gate，继续等待复验 |
| v0.5 | 2026-08-10 | 接入 DEC-WORK-RELAY-001 和首个 CORE 工作项试点；保持验证 pending |
| v0.6 | 2026-08-10 | 在 clean 内容提交 e9c0c55 上完成 sync 与 pilot 结构复验；真实工作项试点仍 pending |
| v0.7 | 2026-08-11 | Human 接受 DEC-SYS-CHUNK-CORE-001；记录原子激活前 pending 状态 |
| v0.8 | 2026-08-11 | 在 clean 提交 981a21e 上完成 activation、sync 和 pilot 结构复验 |
| v0.9 | 2026-08-11 | 在 clean 结果提交 f04568f 上完成 CORE 类型、行为、审计、治理和独立复核；记录关闭合同阻塞 |
| v1.0 | 2026-08-11 | 接入 DEC-WORK-RELAY-002，关闭 CORE 并进入无当前 WI 的选择状态；等待新基线复验 |
| v1.1 | 2026-08-11 | clean 内容提交 df93d97 上 sync/pilot、语义和独立复核通过；首次真实接力试点 verified |
