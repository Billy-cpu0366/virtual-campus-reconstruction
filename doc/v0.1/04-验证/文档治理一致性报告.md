---
title: 文档治理一致性报告
type: governance-verification-report
status: draft
verification-status: pending
version: v0.1
updated: 2026-08-10
baseline-commit: pending
checker-mode: sync
---

# 文档治理一致性报告

> 本文分别记录自动结构检查与 Human/AI 语义审查。自动 PASS 不能代替事实、边界、复用和验收质量判断。本报告只验证 `DEC-DOC-GOV-001` 的治理同步，不通过阶段 6B、治理机制试点或项目最终覆盖验收。

## 1. 验证范围

- 决策：`DEC-DOC-GOV-001`；
- 模式：`sync`；
- 受影响文件：以 [[../需求来源与决策台账#受影响文件同步清单]] 为准；
- 当前阶段：`stage-6B-human-review`；
- 当前 Gate：`GATE-STAGE6B`，`pending-human-review`；
- 禁止范围：旧 Phaser、正式 `src`、阶段 6B/6C 状态推进和证据刷新。

## 2. 自动结构检查

| 项目 | 命令或依据 | 状态 | 结果 |
|---|---|---|---|
| Python 语法与 CLI | `compile(...)`、`--help` | preflight-passed | `PYTHON_COMPILE_PASS`、`CHECKER_CLI_PASS` |
| 治理同步检查 | `python sample/tools/check-doc-governance.py --mode sync` | preflight-passed | dirty transaction 上 596 项结构检查 PASS；clean 内容基线仍待运行 |
| Markdown 与链接 | 同上 | preflight-passed | 围栏、表格、Wiki/Markdown 文件和锚点解析通过 |
| 节点字段与父级 | 同上 | preflight-passed | 12列、概念分组、父级、状态、证据等级、来源和能力映射通过 |
| 来源哈希 | 同上 | preflight-passed | 6份受控外部材料在本机均与台账 SHA-256 匹配 |
| 保护路径 | Git 状态与检查器 | preflight-passed | `sample/original-public-build/` 与 `src/` 零改动 |

## 3. 语义审查

| 问题 | 状态 | 当前结论 |
|---|---|---|
| 原站事实与重构决定是否分开 | preflight-passed | 节点证据等级不允许 `DECISION`；重构决定通过决策引用追踪 |
| 台账是否把培训示例误写成要求 | preflight-passed | 转写登记为 `suggestion`，结构和图片登记为 `example`；只有 Human 会话形成 accepted 决定 |
| 节点范围和候选去向是否符合当前范围 | preflight-passed | 已知 `CAP-*` 全部有节点去向，未来校园/后台保持 out-of-scope，不批量创建详细文档 |
| 当前阶段与 Human Gate 是否未被推进 | preflight-passed | 仍为 `stage-6B-human-review` / `GATE-STAGE6B` / `pending-human-review` |
| 唯一所有权和历史摘要是否清楚 | preflight-passed | task_plan、决策台账、节点总账和正式主定义职责分开；Q-MAP关闭项仅保留审计摘要 |
| 实施成本是否保持最小 | preflight-passed | 复用现有五类目录，只增加1份台账、1份报告和1个标准库检查器 |
| 独立只读复核 | preflight-passed | 首轮发现 §18.1 持久化状态漂移；修正并加固 verified 守卫后复核 PASS，无 blocking/high/medium 缺陷 |

## 4. 未执行的验收

| 验收 | 状态 | 原因 |
|---|---|---|
| `pilot` 地图分块/NPC治理试点 | not-run | 阶段 6B 尚未通过，SYS-CHUNK 尚未进入正式详细设计 |
| `final-coverage` 项目最终覆盖 | not-run | 项目仍处于首个系统选择阶段 |
| 阶段 6B Human Gate | pending-human-review | 本治理同步不自动通过 |
| 阶段 6C | blocked | 阶段 6B 通过前禁止开始 |

## 5. 基线与结果

| 字段 | 值 |
|---|---|
| 内容提交 | pending |
| 受检 Git tree | pending |
| 检查日期 | 2026-08-10 |
| 自动检查 | dirty-tree preflight PASS；clean 基线待执行 |
| 语义审查 | preflight PASS；clean 基线后复核 |
| 独立复核 | preflight PASS；无 blocking/high/medium 缺陷 |
| 最终同步结论 | 已落盘、待 clean 基线验证 |

## 6. 变更后的失效规则

本报告完成后，如决定内容、AI 必读入口、节点/模板合同、当前 Gate、检查器或受检范围发生语义变化，`verification-status` 必须回到 `pending` 并重新生成基线。
