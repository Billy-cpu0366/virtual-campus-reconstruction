---
title: 文档有没有对齐（治理检查报告）
type: governance-verification-report
status: draft
verification-status: pending
version: v0.6
updated: 2026-08-10
baseline-commit: e9c0c552673a5d32d7dea860978c4a214ebac12a
checker-mode: sync
---

# 文档有没有对齐（治理检查报告）

> **一句话：这份文件记录文档链接、状态和规则是否一致；它不证明游戏功能已经正确。**

> 本文分别记录自动结构检查与 Human/AI 语义审查。自动 PASS 不能代替事实、边界、复用和验收质量判断。本报告记录 `DEC-DOC-GOV-001` 与 `DEC-WORK-RELAY-001` 的结构同步；它不自动授权正式代码，也不替代工作项真实试点或项目最终覆盖验收。

## 1. 验证范围

- 决策：`DEC-DOC-GOV-001`、`DEC-WORK-RELAY-001`；
- 模式：`sync`；
- 受影响文件：以 [[../谁确认过什么（来源与决策记录）]] 中对应决定清单为准；
- 当前工作项：`WI-SYS-CHUNK-CORE-001`，`awaiting-authorization`；
- 当前阶段：`implementation-authorization`；
- 当前 Gate：`GATE-SYS-CHUNK-IMPLEMENTATION`，`pending-human-implementation-authorization`；
- 禁止范围：旧 Phaser、正式 `src`、技术栈接受、Phaser集成和证据刷新。

## 2. 自动结构检查

上一治理 clean 基线为 commit `63831821b35cd60d37a3955db642695809c9f889`。本轮首次工作项同步内容提交为 `e9c0c552673a5d32d7dea860978c4a214ebac12a`；提交后已在 clean 工作区复验。

| 项目 | 命令或依据 | 状态 | 结果 |
|---|---|---|---|
| Python 语法 | 内存 `compile(...)` | passed | `PYTHON_COMPILE_PASS`；不生成未跟踪缓存 |
| 治理同步检查 | `python sample/tools/check-doc-governance.py --mode sync` | passed | clean 内容基线 840 项结构检查 PASS（exit 0） |
| 治理试点结构检查 | `python sample/tools/check-doc-governance.py --mode pilot` | passed-structure | clean 内容基线 845 项结构检查 PASS（exit 0）；不等于真实工作项试点完成 |
| Markdown 与链接 | 同上 | passed | 围栏、表格、Wiki/Markdown 文件和锚点解析通过 |
| 当前工作项合同 | 同上 | passed | WI、节点、授权包、设计/验证引用及 pending 授权边界一致 |
| 来源哈希 | 同上 | passed | 6份受控外部材料在本机均与台账 SHA-256 匹配 |
| 保护路径 | Git 状态与检查器 | passed | 正式 `src` 和证据快照零改动；只修正 `src/README.md` 工程说明 |
| 独立复核 | lightweight-verifier 只读复核 | passed-preflight | 无 blocking/high/medium 缺陷；正式代码、证据快照零修改，activation按预期阻塞 |

## 3. 语义审查

| 问题 | 状态 | 当前结论 |
|---|---|---|
| 原站事实与重构决定是否分开 | passed | 节点证据等级不允许 `DECISION`；重构决定通过决策引用追踪 |
| 台账是否把培训示例误写成要求 | passed | 转写登记为 `suggestion`，结构和图片登记为 `example`；只有 Human 会话形成 accepted 决定 |
| 节点范围和候选去向是否符合当前范围 | passed | 已知 `CAP-*` 全部有节点去向，未来校园/后台保持 out-of-scope，不批量创建详细文档 |
| 当前阶段与 Human Gate 是否一致 | pending | 阶段6C已由 Human 通过，当前为 `sys-chunk-implementation-authorization-review` / `GATE-SYS-CHUNK-IMPLEMENTATION` / `pending-human-implementation-authorization`；待新基线复验 |
| 唯一所有权和历史摘要是否清楚 | passed | task_plan、决策台账、节点总账和正式主定义职责分开；Q-MAP关闭项仅保留审计摘要 |
| 实施成本是否保持最小 | passed | 复用现有五类目录，只增加1份台账、1份报告和1个标准库检查器 |
| 独立只读复核 | passed | 首轮发现 §18.1 持久化状态漂移并修正；加固 verified 守卫；clean 基线复核 PASS |

## 4. 未通过与未执行的验收

| 验收 | 状态 | 原因 |
|---|---|---|
| `sync` 当前制度同步 | pending | 待本轮文件同步、检查和 clean 提交 |
| `pilot` 结构表达检查 | 结构已可运行，待当前基线复验 | SYS-CHUNK主定义和NPC来源已具备；自动结构通过不等于真实工作项接力完成 |
| 轻量任务接力真实试点 | in-progress | CORE授权包仍为 proposed；尚未走完授权、激活、实现和关闭 |
| `final-coverage` 项目最终覆盖 | not-passed（按预期） | 当前范围节点尚未全部实现和验证 |
| SYS-CHUNK CORE实施授权 | pending-human-authorization | 本次只建立审查材料，不授权代码 |

## 5. 基线与结果

| 字段 | 值 |
|---|---|
| 当前内容提交 | `e9c0c552673a5d32d7dea860978c4a214ebac12a` |
| 检查日期 | 2026-08-10 |
| 自动检查 | clean 基线：`sync` 840项、`pilot` 845项 PASS |
| 语义审查 | Main 已按 accepted 轻量范围复核；正式代码和技术栈仍未授权 |
| 独立复核 | 提交前只读复核 PASS；无 blocking/high/medium 缺陷 |
| 当前同步结论 | `sync-verified`；制度已持久化，真实工作项 `pilot-verified` 仍待 CORE 授权、激活、实现和关闭 |

## 6. 变更后的失效规则

本报告完成后，如决定内容、AI 必读入口、节点/模板合同、当前 Gate、检查器或受检范围发生语义变化，`verification-status` 必须回到 `pending` 并重新生成基线。

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-08-10 | dirty transaction preflight：596 项检查 PASS，报告保持 pending |
| v0.2 | 2026-08-10 | clean 内容基线 `63831821` 验证完成并记录证据 |
| v0.3 | 2026-08-10 | 阶段6B Gate 变化触发失效规则；保留上一基线为历史，等待复验 |
| v0.4 | 2026-08-10 | 阶段6C通过并进入实现授权审查；更新当前 Gate，继续等待复验 |
| v0.5 | 2026-08-10 | 接入 DEC-WORK-RELAY-001 和首个 CORE 工作项试点；保持验证 pending |
| v0.6 | 2026-08-10 | 在 clean 内容提交 e9c0c55 上完成 sync 与 pilot 结构复验；真实工作项试点仍 pending |
